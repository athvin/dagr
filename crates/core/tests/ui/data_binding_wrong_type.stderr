# Compile-failure snapshot for `data_binding_wrong_type.rs` — ticket T11 (021).
#
# CONTRACT (T8 harness, crates/core/tests/ui.rs): each non-comment, non-blank
# line below is a substring that MUST appear somewhere in the sample's real
# compiler diagnostic. The harness asserts ONLY that the sample FAILS to compile
# and that every substring appears — never exact prose, wording, note count, or
# span layout — so ordinary compiler-message churn does not break the suite
# (C3 message-quality clause; C28 "assert only that both type names appear";
# pinned to the workspace toolchain per rust-toolchain.toml).
#
# Binding a `Handle<Beta>` where the REAL binding API declares `type Input =
# Alpha` is an unsatisfied associated-type bound on the sealed `Deps` trait:
# `<Handle<Beta> as Deps>::Inputs == Alpha`. The substrings are the stable,
# toolchain-pinned facets: the error CODE `E0271` and BOTH type names (`Alpha`
# expected, `Beta` supplied) — the C3 both-type-names contract.
#
# REGENERATION (blessing): rewritten deliberately, never silently, via the
# single documented command from the repo root:
#
#     DAGR_BLESS=1 cargo test -p dagr-core --test ui
#
# (see crates/core/tests/ui.rs). Review this diff on a pinned-toolchain bump.

E0271
Alpha
Beta
