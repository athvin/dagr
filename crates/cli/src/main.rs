//! The `dagr` pipeline-binary entry point (placeholder skeleton).
//!
//! At this milestone it does nothing: the command-line contract (the standard
//! verbs, typed parameters, and the exit-code table of arch.md C26) lands in
//! later tickets (T55, T56). This placeholder exists only so the workspace has
//! the pipeline binary the ADR describes and so `cargo build --workspace`
//! links it.

fn main() {}
