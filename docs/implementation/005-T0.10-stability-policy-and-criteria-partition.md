# 005 · T0.10 — Stability policy and criteria partition

> **Milestone:** M0 · **Size:** S · **Type:** decision · **Components:** system-level (Stability, System-level acceptance)
> **Branch:** `adr/t0.10-stability-policy-and-criteria-partition` · **Depends on:** — · **Blocks:** T7, T4, T39, T48, T65

## Why / context
dagr sells compile-time confidence and honest, replayable artifacts, so the two most expensive things to break are the authoring API and recorded artifacts. This ticket writes the project's binding stability contract — MSRV, semver, additive-only schema evolution, the fixture-corpus plan, and fingerprint-algorithm versioning — and, just as importantly, produces the single source of truth that classifies every acceptance criterion in arch.md as `machine`, `human`, or `disclaimer`. It is governed by arch.md's **Stability**, **Platform support**, **Performance envelope**, and **System-level acceptance** sections, and it locks system-level criterion 8: the criteria matrix that CI enforces. Nothing here is code; it is decisions recorded as a versioned ADR plus a checked-in criteria matrix that later tickets consume. It reshapes T7 (CI matrix scaffold), T4 (schema versioning), T39 (publish schemas), T48 (artifact compatibility CI), and T65 (system acceptance gate), which is why it must land before any of them.

## Objective
Produce a committed ADR and a checked-in criteria matrix that decide and record, unambiguously, the project's stability policy and criterion classification. Concretely:

- **MSRV:** state the pinned minimum supported Rust version, where it is pinned (workspace) and documented (README), and the rule that raising it is a minor version bump called out in release notes.
- **Semantic-versioning policy:** state the authoring-API semver contract (within a major, pipelines keep compiling), and enumerate what constitutes a major event — breaking the authoring API, replacing or major-bumping tokio, breaking recorded-artifact compatibility.
- **Schema-evolution rules:** record the additive-only-within-a-version rule for all three schemas (event stream, graph artifact, run artifact), the reader contract (ignore unknown fields, default missing ones), and how the folding function declares which stream schema versions it reads.
- **Fixture-corpus plan:** define the corpus location in-repo, its contents (one artifact per released schema version per artifact kind, plus the ten-thousand-attempt scale artifact), when it is seeded (M3) and frozen, and the standing CI obligation to parse every prior-version fixture forever after.
- **Fingerprint-algorithm versioning:** record that the structural fingerprint and policy hash each carry a versioned algorithm identifier, that cross-toolchain stability is a tested guarantee, and that an algorithm-version mismatch reads as "cannot compare" (distinct from "topology differs"), never as false difference.
- **Criteria partition:** classify every acceptance criterion in arch.md — all of C1–C28 and the eight system-level criteria — as exactly one of `machine`, `human`, or `disclaimer`, with platform-conditional criteria (limit detection, signal handling, flush behavior) named as such. Record this as the checked-in criteria matrix that seeds the T7 CI coverage matrix.

## Test plan (write these first — TDD)
This is a decision ticket, so the "tests" are validation checks against the recorded ADR and the matrix artifact; each is concrete and independently checkable, and several become executable CI checks that later tickets (T7, T48, T65) wire up.

- **Matrix totality — every criterion appears exactly once.** Setup: the checked-in criteria matrix and a list of every criterion id in arch.md (C1–C28 plus system-level criteria 1–8, including the sub-parts of criterion 4 and criterion 8). Action: cross-reference the matrix against that list. Expected: each criterion id appears in the matrix exactly once, with a classification of `machine`, `human`, or `disclaimer`, and no criterion id is missing or duplicated. A criterion absent from the matrix is a defect this check catches.

- **Classification correctness against the spec's own labels.** Setup: the four arch.md system-level criteria that carry explicit bracketed labels (criteria 1–8, several split machine/human). Action: compare each matrix row against arch.md's stated classification. Expected: criterion 1 is machine (quickstart compiles/runs) plus human (thirty-minute walkthrough); criterion 4 records (a) machine structural, (b) machine interpretive, (c) disclaimer; criterion 8 records the machine coverage-of-machine-criteria part and the human release-checklist part; the human-classed set includes at minimum diagram readability (C24), documentation-at-point-of-use (C21), the thirty-minute walkthrough, and C1's types-readable-from-the-declaration judgment. Each matrix classification matches the spec.

- **Disclaimer criterion is carried unclassified-but-present.** Setup: the matrix and criterion 4(c) — what tasks do against external systems. Action: look up its row. Expected: it is present in the matrix marked `disclaimer`, not `machine` or `human`, and not silently absent.

- **Platform-conditional criteria are named.** Setup: the matrix and the platform-conditional behaviors called out in Platform support (cgroup limit detection C12, signal handling C16, flush/fsync behavior). Action: inspect those rows. Expected: each is flagged as platform-conditional so the T70 platform matrix can gate them, rather than being treated as unconditionally-machine on every platform.

- **MSRV is stated and singular.** Setup: the ADR. Action: read the MSRV decision. Expected: a single concrete minimum Rust version is named, the pinning location (workspace) and documentation location (README) are stated, and the "raise = minor bump, noted in release notes" rule is recorded.

- **Semver major-events list is closed and includes tokio.** Setup: the ADR's semver section. Action: read the enumerated major-version triggers. Expected: the list explicitly includes authoring-API breakage, tokio replacement or major bump, and recorded-artifact incompatibility, matching arch.md's Stability commitments.

- **Schema-evolution rule is additive-only and reader-tolerant.** Setup: the ADR's schema section. Action: read the evolution rule for the three schemas. Expected: within a version, only additive changes are permitted; readers ignore unknown fields and default missing ones; the folding function declares the stream schema versions it reads; a non-additive change is stated to require a version bump.

- **Fixture-corpus plan pins location, contents, seeding, and the forever-parse obligation.** Setup: the ADR's fixture-corpus section. Action: read it. Expected: it names the in-repo corpus location, states one frozen artifact per released schema version per artifact kind, includes the ten-thousand-attempt scale artifact, states seeding at M3, and states the standing CI obligation (consumed by T48) that every prior-version fixture must remain parseable by current tooling.

- **Fingerprint-versioning decision distinguishes "cannot compare" from "topology differs."** Setup: the ADR's fingerprint-versioning section. Action: read it. Expected: it records a versioned algorithm identifier on the structural fingerprint and policy hash, states cross-toolchain stability as tested, and states that an algorithm-version mismatch reads as "cannot compare" — distinct from a topology difference and never a false difference.

- **Reshape hand-off is explicit.** Setup: the ADR. Action: read the section describing downstream impact. Expected: it states, per blocked ticket, exactly what T7 consumes (the classification and the platform-conditional flags), what T4/T39 consume (the schema-version-field and additive-only rules), what T48 consumes (the fixture-corpus plan and forever-parse obligation), and what T65 consumes (the machine/human/disclaimer partition for the acceptance gate), so no downstream ticket has to re-derive these decisions.

- **ADR is committed in the accepted state.** Setup: the repository. Action: locate the ADR file and read its status. Expected: the ADR exists at its recorded path, is in an accepted (not draft/proposed) status, and links to the criteria matrix it produces.

## Definition of done
- [ ] The ADR states a single pinned MSRV, its workspace pin location and README documentation location, and the rule that raising MSRV is a minor version bump announced in release notes (Stability).
- [ ] The ADR records the authoring-API semver contract — within a major version, unchanged pipelines keep compiling — and enumerates the closed set of major-version events (authoring-API breakage, tokio replacement or major bump per C13, recorded-artifact incompatibility) (Stability).
- [ ] The ADR records the additive-only-within-a-version schema-evolution rule for the event stream, graph artifact, and run artifact, the reader contract (ignore unknown fields, default missing ones), and that the folding function declares which stream schema versions it reads (Stability, C22).
- [ ] The ADR records the fixture-corpus plan: in-repo location, one artifact per released schema version per artifact kind, inclusion of the ten-thousand-attempt scale artifact, seeding at M3, and the standing CI obligation that every prior-schema-version fixture is parsed by current tooling forever after (Stability, Performance envelope, C22).
- [ ] The ADR records fingerprint-algorithm versioning: a versioned algorithm identifier on the structural fingerprint and policy hash, tested cross-toolchain stability, and an algorithm-version mismatch that reads as "cannot compare" (distinct from "topology differs") and never as false difference (Stability, C21).
- [ ] The ADR records the tier-1 Linux / dev-supported macOS / unsupported-Windows platform posture and names the platform-conditional criteria (limit detection C12, signal handling C16, flush behavior) as such (Platform support).
- [ ] A checked-in criteria matrix exists that classifies every criterion in arch.md — C1 through C28 and system-level criteria 1–8 — as exactly one of `machine`, `human`, or `disclaimer`, each appearing exactly once (System-level acceptance criterion 8).
- [ ] The criteria matrix matches arch.md's explicit bracketed classifications: criterion 1 machine+human; criterion 4 split into (a) machine-structural, (b) machine-interpretive, (c) disclaimer; criterion 8 split machine+human; the human set includes at least C24, C21, the thirty-minute walkthrough, and C1's declaration-readability judgment (System-level acceptance).
- [ ] Platform-conditional criteria are flagged in the matrix so the platform-matrix job (T70) can gate them per platform rather than treating them as unconditionally machine (Platform support, System-level acceptance).
- [ ] The ADR names the downstream hand-off explicitly: what T7, T4/T39, T48, and T65 each consume from this decision, so those tickets do not re-derive stability or classification decisions.
- [ ] The ADR is committed at its recorded in-repo path in an accepted status and links to the criteria matrix it produces.
- [ ] CI is green on the ticket branch (fmt, clippy with warnings denied, tests, rustdoc lint, and cargo-audit/deny where configured).

## Open questions
None.

## Out of scope
- Building the CI workflow or the matrix-enforcement script — that is T7; this ticket only produces the classification and the matrix content T7 consumes.
- Choosing the concrete serialization format, schema language, or validation crate — that is T4; this ticket fixes only the versioning and additive-only rules, not the encoding.
- Authoring the actual schema documents or emitting artifacts — T39, T40, T42.
- Generating, freezing, or validating the fixture corpus itself and wiring the compatibility CI — T48; this ticket only records the plan and the standing obligation.
- Defining the fingerprint composition, canonicalization, or stable-name trait — that is T0.7; this ticket versions the algorithm and states the "cannot compare" semantics, it does not design the hash.
- Writing any implementation, test harness, or benchmark — the coverage tests, the interpretive-determinism harness (T62), and the scale benchmark (T69) are downstream; this ticket is a decision record.
- Any temptation to make the criteria matrix a live metadata store, a runtime registry, or a machine-readable scheduler input — it is a checked-in, review-owned document; dagr is not and will never be a metadata store or scheduler.
