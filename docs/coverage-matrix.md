# dagr acceptance-criteria coverage matrix

> **Status:** enforcement artifact, authored by ticket T7 (ticket 006,
> [`docs/implementation/006-T7-ci-pipeline-and-criteria-matrix.md`](implementation/006-T7-ci-pipeline-and-criteria-matrix.md)).
> A **checked-in, review-owned data file** — never a runtime registry, a
> metadata store, or a scheduler input (that boundary is permanent; see
> [`docs/arch.md`](arch.md) "When not to use this"). Edited only by pull
> request, reviewed like code.

This file is the **coverage** half of arch.md system-level acceptance
**criterion 8**: *"every machine-classed acceptance criterion … is covered by an
automated test, and that coverage is itself verified in continuous integration
from a checked-in criteria matrix."* It binds each **machine**-classed criterion
to the **test id** that covers it, or to an explicit **`unmapped`** placeholder
while its covering test has not yet shipped. It is verified in CI by
[`scripts/check-coverage-matrix.sh`](../scripts/check-coverage-matrix.sh).

## Two matrices, one boundary

There are two distinct artifacts; do not conflate them:

- [`docs/criteria-matrix.md`](criteria-matrix.md) — the **PARTITION** (ticket
  T0.10): each arch.md criterion labelled `machine` / `human` / `disclaimer`.
  It is the authoritative classification. **This file does not restate or
  re-decide it**; the verifier reads each criterion's class *from the partition*
  and fails if this file disagrees.
- **This file** — the **COVERAGE matrix**: for every criterion the partition
  lists, a row binding a machine criterion to its test id (or `unmapped`), the
  owning ticket, and the platform-conditional flag T70 keys on.

## How the verifier reads this file

[`scripts/check-coverage-matrix.sh`](../scripts/check-coverage-matrix.sh) parses
the table below and **fails CI** when any of these holds (each is exercised by
the verifier's own self-tests,
[`scripts/check-coverage-verifier-selftest.sh`](../scripts/check-coverage-verifier-selftest.sh)):

- a criterion in the partition is **absent** from this matrix;
- a criterion appears on **more than one** row;
- a **machine** criterion is **`unmapped` and owed by this ticket** (`Covered-by`
  is `T7`, empty, or `—`) — its covering test was supposed to exist already;
- a **machine** criterion maps to a **test id that does not exist** in the
  `cargo test --workspace` suite (a dangling reference).

**`unmapped` is the normal early state.** A machine criterion whose covering
test ships in a *later* ticket is legitimately `unmapped` and names that future
ticket in `Covered-by` (quality-gates.md §3). Every component ticket, as it
lands its covering test, edits this matrix to replace `unmapped` with the test
id — that per-ticket duty is what keeps the debt from exploding at the T65
acceptance gate. Human and disclaimer rows never carry a test.

## Column meanings

| Column | Meaning |
|---|---|
| **Criterion** | The criterion id, exactly as classified in the partition (`C1`–`C28`; `SL1`–`SL7` with criterion 4 split into `SL4a`/`SL4b`/`SL4c`; criterion 8 split into `SL8machine`/`SL8human`). |
| **Class** | `machine` \| `human` \| `disclaimer`, mirroring the partition (the verifier fails on any disagreement). |
| **Platform** | `platform-conditional` for the limit-detection (C12), signal-handling (C16), and flush/fsync (C19) criteria, so T70 can attach the Linux-tier-1 and macOS-core jobs later; `—` otherwise. |
| **Test** | For a machine criterion: the mapped test id (as `cargo test -- --list` prints it) **or** `unmapped`. For human: `release-checklist`. For disclaimer: `—`. |
| **Covered-by** | The ticket that ships (or shipped) the covering test. `T7` means "owed by this ticket"; any later tid means the mapping is deferred to that ticket; `—` for human/disclaimer rows. |
| **Notes** | Free text: what the mapped/owed test asserts. |

## Layer A–D criteria (C1–C28)

| Criterion | Class | Platform | Test | Covered-by | Notes |
|---|---|---|---|---|---|
| C1 | human | — | release-checklist | — | "types readable from the declaration" is a judgment (criterion 8). C1's mechanical sub-criteria are machine-tested under T9/T29/T14 but the criterion's governing class is human. |
| C2 | machine | — | ui | T12 | **Mapped by T12** (`crates/core/tests/ui.rs`, test `ui` — the single T8 UI-harness entry point that runs every `tests/ui/*.rs` compile-fail case under the pinned toolchain). C2's compile-fail acceptance is covered by the real-authoring-API cases T12 ships: cycle inexpressibility over **data** edges (`wiring_data_cycle_self_edge`, `wiring_data_cycle_back_edge`, against `dagr_core::flow::Flow::register` — E0425), over **ordering** edges (the adopted T0.9 `ordering_edge_self_cycle` / `ordering_edge_back_edge`), the unforgeable handle (`wiring_handle_unforgeable`, real `dagr_core::handle::Handle` — E0451 private fields) and the no-by-name/index/string lookup (`wiring_handle_no_lookup`, real `Flow`/`Pipeline` — E0599). The handle copy/pass and rename-changes-identity / reorder-changes-nothing node-identity facets are the compile-PASS positive counterparts asserted by `crates/core/tests/typed_handle_positive.rs`, `crates/core/tests/typed_handle_behavior.rs`, and `crates/core/tests/flow_builder.rs` (T10/T13). No longer `unmapped`. |
| C3 | machine | — | ui | T12 | **Mapped by T12** (`crates/core/tests/ui.rs`, test `ui` — the single T8 UI-harness entry point that runs every `tests/ui/*.rs` compile-fail case under the pinned toolchain). C3's compile-fail acceptance is covered by the real-authoring-API (`dagr_core::binding`) cases: wrong-type binding naming BOTH type names (`data_binding_wrong_type` — E0271, `Alpha`+`Beta`; its both-names assertion is proven non-vacuous by the harness's substring contract), wrong arity too-few/too-many (`data_binding_wrong_arity_too_few`/`_too_many` — E0271), the arity-ceiling curated diagnostic at the documented ceiling of 8 (`data_binding_arity_ceiling` — the `#[diagnostic::on_unimplemented]` message, not a trait-error wall), the non-`all-succeeded` trigger-rule typestate on a data-dependent node (`data_binding_non_default_rule` — E0599, the `ConsumesData` state offers no `trigger_rule` method), and the type-level ownership slice (`wiring_owned_receive_not_deliverable` — an owned receive of a `!Send` value fails to compile; the whole-graph multi-consumer conflict is an assembly error deferred to T14). The `all-succeeded` positive counterpart and one-handle-fans-out are in `crates/core/tests/data_binding_positive.rs`. No longer `unmapped`. |
| C4 | machine | — | unmapped | T50 | Registration-time backward-reference edge; both edge kinds recorded; effect-only node has no value; propagation across ordering edges (T50). |
| C5 | machine | — | all_defaults_node_equals_no_policy_under_the_policy_hash | T29 | **Mapped by T29 (039)** — the full C5 node-policy surface lands in `dagr_core::assembly` (`NodePolicy`, resolved `EffectivePolicy`) against the real `Flow`/`Pipeline`/`assemble` path, one arch.md acceptance criterion per test in `crates/core/tests/node_policy.rs`. The mapped headline test (`all_defaults_node_equals_no_policy_under_the_policy_hash`) covers arch.md C5's *"A node with no stated policy behaves identically to one with every default written out — including under the fingerprint's policy hash (C21)"* (structural fingerprints match too); its siblings cover the rest of C5's owned surface: **every field's documented default applied uniformly** → `every_field_has_its_documented_default`; **all-defaults == no-policy field-for-field** → `all_defaults_node_equals_no_policy_node`; **full effective policy incl. defaults reaches the artifact** → `full_effective_policy_including_defaults_is_present`; **a single policy value moves only the policy hash** → `a_single_changed_policy_value_changes_only_the_policy_hash`; **per-attempt timeout is a policy value** → `timeout_feeds_the_policy_hash_not_the_structural_fingerprint`; **trigger rule feeds the structural fingerprint, not the policy hash** → `trigger_rule_feeds_the_structural_fingerprint_not_the_policy_hash`; **group in neither hash** → `group_is_in_neither_hash`; **valid execution-class override on synchronous work assembles** → `valid_execution_class_override_on_synchronous_work_assembles`; **invalid override on await-bound work fails assembly, names the node, and does not short-circuit T14's all-problems reporting** → `invalid_execution_class_override_fails_and_does_not_short_circuit`; **declared cost vector per-pool native units with the working/output-residency memory split** → `declared_cost_vector_carries_native_units_with_the_memory_split`; **policy attached at registration with no task-code change** → `setting_a_policy_value_requires_no_task_code_change`; **retention flag is policy and feeds the policy hash** → `retention_flag_is_policy_and_feeds_the_policy_hash`; **durability flag lives in policy and arms the existing assembly durable-without-contract check** → `durability_flag_arms_the_assembly_contract_check`; **the migrated retry+backoff shape has one home and produces the runner's `RetryConfig`** → `policy_owns_retries_and_backoff_and_produces_the_runner_config`. The invalid-execution-class-override assembly facet is also covered by T14's `crates/core/tests/assembly.rs` (`invalid_execution_class_override_fails_assembly`), which stays green over the expanded policy. **Deferred (Out of scope):** runtime consumption of these knobs — admission/capacity against the cost vector (T31/C12), execution-class dispatch (T33/C13), and trigger-rule runtime (T34/C15) — and the concrete BLAKE3 hash algorithm + graph-artifact schema (T41/T40/C21) are downstream; T29 defines only the values they read. No longer `unmapped`. |
| C6 | machine | — | unmapped | T46 | Unique names across grouping; diagram clusters by group; group rename changes no execution behaviour and no fingerprint (T46/T51). |
| C7 | machine | — | assembling_twice_yields_byte_identical_graph_artifacts | T14 | **Mapped by T14 (025)** — the assembly-validation-and-precomputation ticket ships the C7 machine acceptance criteria T13 deferred, in `crates/core/tests/assembly.rs` against the real assembly pass `dagr_core::flow::Pipeline::assemble` (`dagr_core::assembly`). The mapped headline test (`assembling_twice_yields_byte_identical_graph_artifacts`) covers arch.md C7's *"assembling the same pipeline twice … produces byte-identical graph artifacts"* (generation-time field aside); its siblings in the same suite cover the rest of C7's owned surface, one arch.md acceptance criterion each: **duplicate-name failure naming both declarations** → `duplicate_node_name_names_both_declarations`; **empty-environment assembly (assembly succeeds with every external resource absent)** → `assembly_is_pure_runs_in_an_empty_environment`; **all-problems-reported-not-just-the-first** → `all_problems_are_reported_not_just_the_first` (+ `fixing_one_defect_still_surfaces_the_rest`); **exact consumer counts before execution** → `consumer_counts_are_exact_before_execution`; **no-parameter-reachable** → a *structural* fact (the `AssemblyArtifact` surface exposes no parameter accessor — see the module rustdoc), demonstrated by `assembly_is_pure_runs_in_an_empty_environment` assembling with no parameters present. Further assembly checks land as their own tests: empty-pipeline (`empty_pipeline_is_rejected`), invalid class override (`invalid_execution_class_override_fails_assembly`), durable-without-contract (`durable_node_without_the_contract_fails`), ownership-mode conflicts (`owned_demand_on_a_multi_consumer_value_fails`, `owned_edge_into_a_retrying_node_without_clone_on_read_fails`), nonzero teardown cost (`nonzero_teardown_cost_fails`), the zero-consumer warning (`zero_consumer_non_unit_output_is_a_warning_not_an_error`), remaining-dependency counts (`remaining_dependency_counts_match_the_graph`), topological order (`execution_order_is_a_valid_topological_order`), and the empty-by-default env-capture allowlist (`environment_capture_allowlist_is_empty_by_default`). **Still deferred to T15:** the C7 acceptance line *"Bootstrap rejects, before any node executes: a declared cost no pool can ever satisfy, a missing declared resource, an invalid parameter"* is a **bootstrap** check (T0.5 ADR §5/§7 moved cost-fit off assembly), not assembly's — T14 deliberately makes no capacity/cost-fit check, so that facet stays owned by the bootstrap-runtime ticket (T15/T24/T29) and is not covered here; the single `Test` cell maps the assembly facet T14 owns while the note records the deferred bootstrap facet. **T13 (023)** shipped the flow builder and node identity (`dagr_core::flow`), which this ticket validates. The rename-changes-identity / reorder-changes-nothing *node-identity* facet remains owned by **T12**'s wiring compile-fail suite (see the C2 row). **Determinism/purity regression guards added by T15 (026)** — additive, not re-mapping the `Test` cell (which stays T14's headline test): `crates/core/tests/determinism_and_purity.rs` pins the C7 determinism and purity acceptance criteria against the same real assembly pass, one facet per test — byte-identical twice, generation-time masked (`two_in_process_assemblies_are_byte_identical`); generation-time is the sole (empty) mask (`generation_time_is_the_only_masked_difference`); registration order does not change the artifact (`registration_order_does_not_change_the_artifact`); consumer counts exact per node + all precomputed runtime data identical across two assemblies (`precomputed_runtime_data_is_identical_across_assemblies`); assembly succeeds in an empty environment via a scrubbed child process (`assembly_succeeds_in_an_empty_environment`) with a non-vacuous negative control proving the empty-CWD guard bites (`negative_control_stray_filesystem_write_is_detected`); no parameter value reachable during assembly (`no_parameter_value_is_reachable_during_assembly`); and determinism-and-purity combined under the scrub — the C20 CI/PR condition (`empty_environment_determinism_combined`). The mechanical no-filesystem/no-network proof convention (std-only scrubbed-child-process + negative control) is recorded in ticket 026's decision record and reused by T40 / the criteria-matrix structural-determinism job. |
| C8 | machine | — | all_fields_populated_on_a_hand_built_context | T16 | **Mapped by T16** (`crates/core/tests/run_context.rs`): a hand-built `RunContext` populates *every* field on the first attempt and each accessor returns exactly the supplied value — the headline C8 acceptance ("every field populated on every invocation, including the first attempt of the first node"). Sibling tests in the same file cover the rest of C8's owned surface: attempt/max readable (`attempt_number_reflects_the_supplied_attempt`), data interval recorded verbatim (`data_interval_is_carried_verbatim_and_never_interpreted`), and the no-mutation/no-scheduling API shape (`context_exposes_no_mutation_or_scheduling_authority`). The remaining acceptance facet — the attempt number *incrementing across retries* and *appearing in logs and artifacts* — is out of T16's scope (T16 only exposes the field); it is demonstrated end-to-end by the retry runner (T22) and node metrics/artifacts (C22/C23), which map their own rows. |
| C9 | machine | — | a_missing_declared_resource_fails_bootstrap_naming_the_resource_and_nodes | T30 | **Mapped by T30** (`crates/core/tests/resource_registry.rs`, headline test `a_missing_declared_resource_fails_bootstrap_naming_the_resource_and_nodes`) against the real registry `dagr_core::context::ResourceRegistry` and `ResourceRegistry::validate_requirements`, covering arch.md C9's *"a run whose pipeline declares an unregistered resource fails at bootstrap, before any node executes, naming both the missing resource and the nodes requiring it"* — asserting the named missing resource type and the **exact set** of requiring node ids. The sibling facets in the same suite each cover one arch.md C9 acceptance line: **bootstrap-failure artifact produced, distinct from assembly failure, with zero attempts** → `a_missing_resource_produces_the_bootstrap_failure_artifact_with_zero_attempts`; **all-requirements-satisfied passes** → `all_requirements_satisfied_passes_validation`; **registry immutable / shared read-only in-run** → `a_built_registry_is_shared_read_only`; **any resource replaceable by a fake with no task change** → `a_fake_resource_substitutes_with_no_task_change` (with `a_task_reads_a_resource_through_the_run_context` proving the task reaches it through the C8 context); **two same-typed resources distinguished by newtype** → `newtype_wrappers_disambiguate_two_same_typed_resources`; **ambiguous duplicate rejected, first not replaced** → `duplicate_same_type_registration_is_rejected_as_ambiguous` (+ `a_rejected_duplicate_does_not_replace_the_first`); **type-keyed retrieve/miss** → `retrieve_by_type_returns_the_registered_resource` / `retrieving_an_unregistered_type_yields_none`; **declared requirements surfaced for the graph artifact** → `declared_requirements_are_surfaced_for_artifact_emission`; **marked secret never appears on a framework-controlled path (planted sentinel)** → `a_secret_sentinel_never_appears_in_a_framework_emission_path`. The **compile-time** guarantees are the T8 UI harness's (`crates/core/tests/ui.rs`, test `ui`): the secret wrapper's absent `Debug`/`Display` path (`tests/ui/secret_no_debug.rs`, `tests/ui/secret_no_display.rs` — E0277) and the `Send + Sync + 'static` stored-resource bound (`tests/ui/registry_non_send_resource.rs` — E0277). **Deferred beyond T30:** end-to-end framework-emitted log-line redaction is C25/T45 (this ticket ships the wrapper + sentinel hook only); rendering the graph/bootstrap-failure artifact is C20/C22 (this ticket produces the outcome value, `dagr_core::context::BootstrapFailure`, and asserts it is produced). No longer `unmapped`. |
| C10 | machine | — | release_fires_only_after_every_consumer_terminal_and_returned | T17 | **Mapped by T17 (027)** — the output-slot substrate ships C10's owned machine acceptance in `crates/core/tests/output_slot.rs` against the real slot API (`dagr_core::slot`: `Slot`/`SlotRef`/`ConsumerLease`/`ResidencyLedger`/`RedemptionHandle`). The mapped headline test (`release_fires_only_after_every_consumer_terminal_and_returned`) covers arch.md C10's load-bearing release rule — *"after the final consumer reaches a terminal state and every consumer's work has returned, that node's value is unreachable and its memory returned to the allocator"* — and its `release_waits_on_closure_return_not_terminal_decision` / `zombie_consumer_pins_value_and_residency` siblings pin the zombie-aware half (*"while an abandoned consumer's closure is still running, the residency stays counted"*). Siblings in the same suite cover the rest of C10's owned surface, one acceptance criterion each: **never-read-before-fill loud node-named defect** → `read_before_fill_is_a_loud_node_named_defect`; **once-writable** → `second_fill_is_rejected_original_unchanged` (+ `fill_once_then_read_the_value`); **shared retry finds the value intact** → `shared_consumer_retry_finds_the_value_intact`; **retained redeemable / released not redeemable** → `retained_value_survives_to_run_end_and_is_redeemable`, `released_value_is_not_redeemable`, `redeeming_an_unfilled_slot_reports_never_filled_not_released`; **single-count residency** → `residency_is_counted_once_not_per_consumer`; **peak measured slot residency (accounting hook)** → `accounting_hooks_expose_peak_residency`; **no-lookup/no-type-check read path** → `no_lookup_no_type_check_on_the_read_path`; the three T0.2 delivery modes → `owned_delivery_moves_the_value_out`, `clone_on_read_gives_each_attempt_a_fresh_value`. **Bounded-memory facet added by T26 (036)** — additive, not re-mapping the `Test` cell (which stays T17's release-rule headline): `crates/cli/tests/bounded_memory_chain.rs` ships C10's authoritative *peak-memory-flat over a **hundred-node** chain* assertion (*"Peak allocator-level memory across a long chain does not grow with the chain's length when nothing is retained — verified against a synthetic hundred-node chain"*), driving the real merged pieces — real T17 output slots (`Slot`/`SlotRef`/`ConsumerLease`/`ResidencyLedger`/`RedemptionHandle`) run to completion through the **real** T24 driver (`dagr_cli::driver::drive`) on a shared ledger. Two allocator-level instruments, both deterministic and never RSS: the exact per-run `ResidencyLedger` peak (the load-bearing integer) and a test-only `#[global_allocator]` recording live/peak allocated bytes. One arch.md facet per test: **peak is flat across chain length (short vs hundred-node)** → `ledger_peak_is_flat_across_chain_length` (+ the allocator-level restatement `allocator_peak_is_flat_across_chain_length`); **peak bounded by a few concurrent values, not the whole chain** → `ledger_peak_bounded_by_a_few_values_not_the_whole_chain`; **value released after the sole consumer is terminal-and-returned** → `value_released_after_sole_consumer_terminal_and_returned`; **retained survives & is redeemable while released ones are not** → `retained_value_survives_and_is_redeemable_released_ones_are_not`; **determinism / no flakiness** → `ledger_peak_is_deterministic_across_repetitions`; and a non-vacuity guard proving a chain with release defeated peaks proportionally to length → `peak_grows_with_length_when_release_is_defeated`. T17's smaller in-crate smoke (`chain_peak_does_not_grow_with_chain_length`) still guards the release logic in `crates/core`. **Deferred to C23 (T44):** *measured peak slot residency appearing in the run artifact* — T17 exposes, and T26 asserts, the `ResidencyLedger` peak hook the artifact folds, but rendering that number into the run-artifact summary is C23's. The single `Test` cell maps the release-rule facet T17 owns while the note records the T26-landed bounded-memory facet and the deferred C23 facet. |
| C11 | machine | — | diamond_proves_no_wave_batching | T18 | **Mapped by T18 (028)** — the readiness tracker ships C11's owned machine acceptance in `crates/core/tests/readiness.rs` against the real tracker (`dagr_core::readiness`: `ReadinessTracker`/`Decision`/`evaluate_rule`/`RuleOutcome`) driving synthetic pipelines with injected terminal outcomes (no runtime, no clock). The mapped headline test (`diamond_proves_no_wave_batching`) covers C11's load-bearing acceptance — *"in a diamond with one slow branch, the fast branch's descendants are not delayed by the slow branch unless they depend on it"* and *"work is never batched into waves"* — the fast branch's independent descendant is emitted ready before the slow branch reaches any terminal state and while the join stays not-ready. Siblings in the same suite cover the rest of C11's owned surface, one acceptance criterion each: **countdown seeds from T14's precomputed counts / source frontier** → `countdown_seeds_from_precomputed_dependency_counts`, `source_nodes_are_ready_without_any_notification`; **decrement-on-terminal unlocks exact dependents** → `decrement_on_terminal_unlocks_the_exact_dependents`; **all-upstreams-terminal evaluation gate (no partial fire)** → `rule_is_not_evaluated_on_a_partial_result`; **`all-succeeded` fires on the last upstream** → `all_succeeded_fires_when_the_last_upstream_completes`; **can-never-fire → propagated terminal per the T0.4 §5a table, carrying the originating node** → `all_succeeded_can_never_fire_upstream_failed`, `all_succeeded_can_never_fire_upstream_skipped`, `all_succeeded_can_never_fire_cancelled`, `mixed_non_success_classes_propagate_upstream_failed`, `propagated_upstream_skipped_counts_skip_like`; **`satisfied-from-prior` counts success-like** → `satisfied_from_prior_counts_success_like`; **propagated-terminal cascade without intervening execution** → `propagated_terminal_cascades`; **exactly-one-terminal-state / no double assignment** → `every_node_ends_in_exactly_one_terminal_state`, `a_decided_node_is_not_assigned_a_terminal_state_twice`; **pending accounting reaches zero at run end** → `pending_accounting_reports_run_completion`; and the **full three-rule seam (`all-succeeded`/`all-terminal`/`any-failed`) present though only `all-succeeded` fires in M1** → `evaluate_rule_all_succeeded_matches_the_table`, `evaluate_rule_all_terminal_always_fires`, `evaluate_rule_any_failed_matches_the_table`. **Now mapped by T25 (035)** (was "deferred to T25"): the randomized-DAG, randomized-outcome **deadlock-freedom property test** (arch.md C11 *"a test over randomly generated graphs with randomized outcomes confirms every run terminates"*) ships in T25 — see the additive T25 paragraph below. The **`all-terminal` / `any-failed` runtime firing** on runtime nodes (stop-mode contingency admission and the like) is C15/T34, which reuses the same `evaluate_rule` seam; T18 wires only `all-succeeded` onto runtime nodes. The single `Test` cell maps the no-wave-batching facet T18 owns while the note records the T25-landed termination facet and the deferred T34 facet. **Run-loop-driven facet added by T24 (034)** — additive, not re-mapping the `Test` cell (which stays T18's headline test): `crates/cli/tests/run_loop_driver.rs` drives the *real* tracker through the M1 run-loop driver (`dagr_cli::driver::drive`) end-to-end, proving C11's readiness half at runtime rather than against injected outcomes — the no-wave-batching guarantee driven for real (`fast_branch_not_gated_on_slow_branch`: a diamond whose fast-branch descendant is admitted before the slow branch terminates), decrement-and-admit on individual completion (`linear_chain_drives_dependents`, `every_outcome_is_fed_back`: each dependent's `node-ready` follows only its upstream's `node-terminal`), propagated-terminal-without-execution driven for real (`failing_upstream_propagates_and_run_fails`: the deadened downstream ends `upstream-failed` and emits no `attempt-started`), and precise termination when nothing is pending or in flight (`run_ends_when_nothing_pending_or_in_flight`). **Termination-property facet added by T25 (035)** — additive, not re-mapping the `Test` cell (which stays T18's headline test): `crates/core/tests/termination_property.rs` ships C11's anti-deadlock acceptance line — *"The tracker cannot deadlock: a test over randomly generated graphs with randomized outcomes confirms every run terminates."* It drives the *real* `ReadinessTracker` (the crux state machine) over thousands of hand-generated valid acyclic DAGs (bounded node count, acyclic lower→higher edges, mixed fan-in arities to the C3 ceiling, `all-succeeded` runtime nodes) with randomized per-node outcomes (success, permanent failure, deliberate skip, timeout, retry-then-succeed→success), mirroring the T24 admit→feed-back→admit loop, and asserts all four invariants as emergent properties over random shapes: **termination within a bounded step budget** — a budget exhaustion is treated as a deadlock and fails the property (`every_generated_run_terminates_and_holds_the_invariants`); **exactly one normative-taxonomy terminal state per node** (never off-taxonomy, never `not-requested`); **the run-boundary property** — nothing pending at run-finished and no node terminating after it; and **propagation consistency** — never-ran propagated nodes (`upstream-skipped`/`upstream-failed`) are never executed, executed-class nodes run exactly once. Pinned regression cases: the mixed-rule diamond (`regression_mixed_rule_diamond`, with the `all-terminal`-fires-downstream-of-failure counterpart at the `evaluate_rule` seam), the all-skips-succeeds graph (`regression_all_skips_graph`), and recorded-seed replay proving byte-for-byte reproducibility (`replay_recorded_seed`); an opt-in meta-test (`shrinking_produces_a_minimal_counterexample`, `#[ignore]`) documents that the property bites and the shrinker minimises. The suite uses a hand-rolled dependency-free deterministic seeded generator (the ticket's "or equivalent" to proptest — no new dependency, audit/deny untouched); the case count is env-tunable (`DAGR_TERMINATION_CASES`, higher in CI than local) and every case is seed-replayable. A companion driver-level check `crates/cli/tests/termination_property_driver.rs` (`generated_runs_terminate_through_the_real_driver`) drives a small fixed set of the same generated DAGs through the *real* T24 `dagr_cli::driver::drive` against fake scripted runners, proving the full two-runtime run loop terminates too. The `all-terminal`/`any-failed` *runtime firing* over random shapes stays deferred to T34. |
| C12 | machine | platform-conditional | unmapped | T31 | **Limit detection is platform-conditional** (cgroup v2→v1→host). Capacity invariant incl. abandoned-but-running; over-capacity fails at bootstrap; atomic multi-pool acquisition; starvation; pinning override (T31/T32/T37, gated by T70). |
| C13 | machine | — | unmapped | T33 | Long sync task doesn't delay await-bound work; concurrent compute ≤ pool size; all-workers-blocked timeout still fires and SIGTERM still yields a complete stream (T33). |
| C14 | machine | — | successful_attempt_fills_the_slot | T20 | **Mapped by T20 (030)** — the single-attempt execution core ships C14's *single-attempt* machine acceptance in `crates/core/tests/single_attempt.rs` against the real runner `dagr_core::execution::run_attempt` (with `AttemptOutcome` / `AttemptEvent` / `AttemptEventSink`), driven on a hand-built C8 `RunContext` and a capturing C19-shaped sink with no runtime. The mapped headline test (`successful_attempt_fills_the_slot`) covers the load-bearing path — run one attempt, classify success, fill the once-writable output slot (C10), and emit `attempt-started` then `attempt-succeeded`. Siblings in the same suite cover the rest of the T20-owned surface, one acceptance facet each: **exactly one attempt-outcome record per attempt including failures/skips** → `exactly_one_attempt_outcome_record_on_success` + `every_outcome_yields_exactly_one_attempt_outcome_record`; **a permanent error is never retry-eligible regardless of remaining attempts** → `permanent_failure_is_not_retry_eligible_even_with_attempts_remaining` (+ `permanent_failure_does_not_fill_the_slot`); **a retry-eligible error is classified distinctly so T22 can act** → `retry_eligible_failure_is_classified_distinctly`; **a deliberate skip is an originated skip, distinct from success/failure** → `deliberate_skip_is_classified_as_originated_skip`; **non-success never fills the slot** → the failure/skip tests above; **attempt number/max carried through span + events + outcome, and the first attempt carries one** → `attempt_number_is_carried_through_identity` + `first_attempt_carries_attempt_one`; **the span opens before the work runs** → `the_span_opens_before_the_work_runs`; **ordered per-transition events with gapless sequence + node-terminal carries the classified state** → `event_ordering_within_an_attempt` + `node_terminal_record_carries_the_classified_state`; **the value type flows unchanged into the slot (C10)** → `value_type_flows_unchanged`; **runnable in isolation with no runtime** → `runnable_in_isolation_with_no_runtime`. **Per-attempt timeout facet added by T21 (031)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/attempt_timeout.rs` pins C14's per-class timeout acceptance against the real T21 API (`dagr_core::execution::run_attempt_with_timeout` for the await-bound race + `TimeoutDecision` / `LateResultBarrier` / `ZombieObserver` for the blocking/compute mark), runtime-agnostic (the attempt future raced against a caller-provided deadline future, no tokio), one arch.md acceptance facet per test: **await-bound over its timeout → cancelled and permit released immediately** → `await_bound_timeout_cancels_immediately_and_releases_permit`; **blocking over its timeout → recorded timed-out immediately, permit held until the closure returns** → `blocking_timeout_marks_immediately_and_holds_permit_until_return` (+ `compute_timeout_behaves_identically_to_blocking` for the class-shape-driven identical compute case); **retry starts only after the closure returns (C1 exclusivity)** → `retry_of_a_timed_out_blocking_node_is_deferred_past_zombie_return`; **an abandoned attempt's late result never fills a slot and never writes scratch** → `late_result_never_fills_the_slot` + `late_result_never_writes_scratch`; **exactly one attempt-outcome record for a timed-out attempt** → `exactly_one_attempt_outcome_record_for_a_timed_out_attempt`; **timeout retry-eligible by default; a timeout on the final permitted attempt yields terminal `timed-out`** → `timeout_is_retry_eligible_by_default`; **terminal state decided exactly once — `timed-out`, never `abandoned`** → `terminal_state_is_decided_exactly_once`; **a within-timeout attempt is unchanged from T20** → `well_behaved_attempt_within_timeout_is_unaffected`; **the timeout fires regardless of task-worker availability (isolated timer path not regressed)** → `timeout_fires_independent_of_task_worker_availability`. **Retry + jittered-backoff facet added by T22 (032)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/retry_backoff.rs` pins C14's bounded-retry and jittered-exponential-backoff acceptance against the real T22 API (`dagr_core::execution::run_with_retries` for the loop + `RetryConfig` / `Backoff` / `Jitter` / `NoJitter` / `SeededJitter` for the interim knob and injectable jitter, plus the `AttemptEvent::BackoffStarted` phase record), runtime-agnostic (the backoff wait awaits a caller-provided timer future, deterministic seeded jitter — no tokio, no wall-clock sleep), one arch.md acceptance facet per test: **a retry-eligible error is retried up to the configured count and no further** → `retry_eligible_is_retried_up_to_the_budget_and_no_further` (+ `a_single_successful_retry_stops_the_loop`); **a permanent error is not retried regardless of remaining attempts** → `permanent_error_is_never_retried`; **a deliberate skip is not retried** → `deliberate_skip_is_not_retried`; **conservative no-retry default (a single attempt)** → `default_config_performs_exactly_one_attempt`; **backoff is exponential in the attempt index and clamped to the cap** → `backoff_is_exponential_and_capped` (+ `jittered_delay_never_exceeds_the_cap`); **backoff delays are jittered so a fan-out of simultaneous retries does not resynchronize** → `jittered_backoff_does_not_resynchronize_a_fan_out` (+ `seeded_jitter_is_reproducible_and_seed_dependent` for the determinism that makes the schedule assertable); **every attempt produces exactly one attempt-outcome record with gapless increasing attempt numbers, and the terminal node record appears once** → `exactly_one_attempt_outcome_record_per_attempt`; **the backoff interval is a named, monotonic-offset-derived phase distinct from executing** → `backoff_phase_is_a_named_measurable_interval`; **the current attempt number and maximum are exposed to the task (C8)** → `attempt_number_is_visible_to_the_task`; **the next attempt is not started until the prior attempt's closure returned (C1 exclusivity)** → `no_premature_re_entry_attempts_are_strictly_sequential`. **Panic-containment facet added by T23 (033)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/panic_containment.rs` pins C14's **Panics**-paragraph acceptance against the real T23 API (`dagr_core::execution::run_attempt_caught` for the caught single attempt + `run_with_retries_caught` for panic-composed-with-retry, `install_panic_hook`, `check_panic_strategy` / `detect_panic_strategy` / `PanicStrategy`, plus the `AttemptOutcome::Panicked` classification and the `AttemptEvent::AttemptPanicked` outcome record), runtime-agnostic (a dependency-free, `unsafe`-free `catch_unwind` + `AssertUnwindSafe` adapter over the future's poll — no `futures` crate), one arch.md acceptance facet per test: **a panicking task is caught (not unwound) and fails only its own node, mapping to `failed`** → `panicking_task_is_contained_and_fails_only_its_node` (+ `panic_message_is_captured` for the captured detail); **the rest of the run proceeds after a panic** → `independent_node_completes_after_a_panic`; **a panic is a permanent failure, never retried regardless of remaining budget** → `a_panic_is_permanent_and_is_never_retried`; **the panic is attributed to the correct node (task-local state), stable under concurrency** → `panic_is_attributed_to_the_correct_node`; **exactly one attempt-outcome record for a panicking attempt** → `exactly_one_attempt_outcome_record_for_a_panic`; **the binary refuses `panic = "abort"` at startup naming the fix, and permits the unwinding profile** → `startup_check_refuses_abort_and_permits_unwind` (+ `detected_strategy_of_this_binary_passes`); **the hook installs once idempotently and safely under concurrent first-use** → `hook_installation_is_idempotent_and_thread_safe`; **the framework hook chains to / coexists with a pre-existing (test-harness) hook** → `framework_hook_chains_to_a_pre_existing_hook`; **resource poisoning keeps a mid-operation resource out of rotation (author pattern, worked example)** → `resource_poisoning_keeps_a_broken_resource_out_of_rotation`. The `AssertUnwindSafe`-is-load-bearing compile fixture (positive compiles, negative `compile_fail`) ships as doctests on `run_attempt_caught`. **Still deferred (the rest of C14 is owned by later tickets):** the all-workers-blocked timeout-still-fires + SIGTERM-complete-stream *dispatch/isolation* assertion (T33) and the full permit-release outcome matrix under real pools (T37). The single `Test` cell maps the single-attempt facet T20 owns while the note records the T21-landed timeout facet, the T22-landed retry/backoff facet, the T23-landed panic-containment facet, and the deferred T33/T37 facets. No longer `unmapped`. **Run-loop-driven facet added by T24 (034)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/cli/tests/run_loop_driver.rs` drives the *real* C14 attempt path (`run_attempt` / `run_attempt_caught` / `run_attempt_with_timeout`) end-to-end through the M1 run-loop driver, proving the runner runs under a real two-runtime dispatch (attempts on a tasks runtime, the loop/timers/writer on an isolated framework runtime) rather than on a bare test executor: a successful attempt fills the slot and drives its dependent (`happy_path_single_node_terminates`, `linear_chain_drives_dependents`), a deliberate skip terminates the node and the run succeeds (`skip_only_run_reports_success`), and — the framework-isolation half — a per-attempt blocking timeout still fires while a task jams its worker, the stream is still written, and the run still reaches `run-finished` (`framework_survives_a_misbehaving_task`), with the timed-out closure treated as a decided zombie that does not hold the run open and stays `timed-out` (never a second terminal state — `zombie_at_natural_run_end`). The all-workers-blocked *dispatch/starvation* authority and the full permit-release outcome matrix stay deferred to T33/T37. |
| C15 | machine | — | unmapped | T34 | Stop-on-first-failure runs firing contingency nodes; continue-independent completes unrelated branches; no node runs with an unsucceeded data dep; exactly-one-terminal-state; skip-only run is success (T34). |
| C16 | machine | platform-conditional | unmapped | T35 | **Signal handling is platform-conditional** (SIGTERM/SIGINT; Linux fully, macOS documented divergences). Complete stream on termination within budget; `cancelled`/`abandoned` distinct from `failed`; grace/teardown budget; temp cleanup (T35/T36, gated by T70). |
| C17 | machine | — | unmapped | T52 | Teardown runs after any terminal state; failing teardown recorded but doesn't change outcome; runs under a fresh signal even when cancelled; resume re-executes teardown-covered nodes (T52). |
| C18 | machine | — | unmapped | T53 | Value written attempt-one readable attempt-two; keys namespaced by run+node; cross-node read enforced; durable-store resume carry-forward; I/O failure retry-eligible (T53/T54a/T54b). |
| C19 | machine | platform-conditional | envelope_completeness | T19 | **Mapped by T19** (`crates/artifact/tests/event_stream_writer.rs`, test `envelope_completeness`): every record carries run id + schema version + gapless sequence + wall stamp + monotonic offset, over the append-only JSONL writer. The rest of the C19 suite in the same file asserts the sibling facets — `gapless_strictly_increasing_sequence`, `offsets_are_monotonic_and_authoritative`, `run_started_carries_full_header`, `write_through_not_buffered`, `fsync_once_at_run_end`/`fsync_once_at_cancellation`, `abrupt_kill_parseability` (≤1 trailing partial), `concurrent_run_disjointness` (disjoint simultaneous streams), `mid_run_sink_failure_surfaces_fault`, `foldability_with_no_original_run_access`, `canonical_bytes_are_deterministic_and_sorted` (T4 §6). **Flush/fsync behavior stays platform-conditional** (different fsync semantics on macOS; the ≤1-trailing-partial tolerance is the platform-sensitive facet), and the abrupt-process-kill/fault-injection suite (T27) and the end-to-end two-runs test (T67) still layer on top, flush facet gated by T70. No longer `unmapped`. **Run-loop-driven facet added by T24 (034)** — additive, not re-mapping the `Test` cell (which stays T19's headline test): `crates/cli/tests/run_loop_driver.rs` drives the *real* C19 writer through the M1 run-loop driver, proving the driver-owned records the writer defines but does not emit itself — `run-started` carrying the full header (identity, pipeline, both fingerprints, parameters, data interval, allowlisted env) written **before** assembly is acted on so an assembly failure still records itself (`run_started_header_carries_start_fields`, `assembly_failure_still_records`), the `node-ready` / `node-admitted` / attempt-transition / `node-terminal` sequence in order (`happy_path_single_node_terminates`), the `zombie-at-exit` event before `run-finished` (`zombie_at_natural_run_end`), a UUIDv7 identity minted at bootstrap and operator-overridable on every record (`identity_is_a_uuidv7_minted_at_bootstrap`, `operator_override_replaces_the_minted_identity`), the allowlisted-env-only capture with a non-allowlisted-secret negative check (`allowlisted_env_captured_others_not`), and two concurrent runs writing disjoint, independently-valid streams each carrying its own identity (`two_simultaneous_runs_do_not_interfere` — the driver-side counterpart of T19's writer-level `concurrent_run_disjointness`, ahead of the dedicated T67 two-runs test). **Crash-safety / I/O fault-injection facet added by T27 (037)** — additive, not re-mapping the `Test` cell (which stays T19's headline test): `crates/artifact/tests/crash_safety_fault_injection.rs` ships the C19 crash-tolerance and sink-failure acceptance criteria the earlier suites only smoke-tested, driving the **real** merged T19 writer (`dagr_artifact::event_stream::{EventStreamWriter, read_records, SinkFault}`) through an **injected controllable fault sink** (the T0.6 §1 seam) and asserting against the real tolerant reader. A "crash" is simulated **deterministically** — the genuine writer's on-disk bytes truncated at **seeded** offsets *at / before / part-way through* each event write (no real process kill, no wall-clock/network; every randomized trial records its seed for replay) — so the acceptance holds without CI-flaking. One arch.md C19 facet per test: **abrupt kill → valid prefix** across seeded trials (`abrupt_kill_at_a_random_point_yields_a_valid_prefix`); **≤1 trailing partial, never two** around every write, with the reader's tolerance guarded against being too lax (`kill_mid_write_leaves_at_most_one_partial_never_two`); **gapless-and-strictly-increasing sequences after a kill** (`sequence_numbers_are_gapless_after_a_kill`); **a one-record prefix still identifies its run** via the full run-started header (`a_one_record_stream_still_identifies_its_run`); **every record carries run identity + schema version** (`every_record_carries_run_identity_and_schema_version`); **interior corruption is rejected** (`interior_corruption_is_rejected`); the **induced mid-run failing sink surfaces the documented `event stream unwritable` fault by cause, leaving no sequence gap and a valid gapless prefix** (`induced_mid_run_failing_sink_surfaces_the_sink_failure_cause`); **disk-full mid-run behaves as a sink failure** (`disk_full_mid_run_behaves_as_a_sink_failure`); **disk-full at final flush reports the sink-failure cause, not a corrupt success** (`disk_full_at_final_flush_produces_the_sink_failure_cause`); **slow/unwritable sink at shutdown is a bounded wait within a fixed timeout, never a hang** (`slow_or_unwritable_sink_at_shutdown_is_a_bounded_wait_not_a_hang`); **the sink-failure code is asserted by its documented cause, never a magic number** (`sink_failure_is_asserted_by_cause_not_by_literal`); **a genuine run failure is not masked by the self-inflicted `cancelled` a sink fault drives** (`run_failure_is_not_masked_by_self_inflicted_cancellation`); **two concurrent runs write disjoint, individually-valid streams that partition cleanly by run identity** (`two_concurrent_runs_produce_disjoint_individually_valid_streams`); and **the seeded kill points are reproducible** so a failure replays (`seeded_kill_points_are_reproducible`). Because the merged T24 driver deliberately absorbs a sink fault (T24 scoped the cancel-and-exit *reaction* out to T36/T55), the "moves to cancelling / exits with the sink-failure code" contract is asserted at the writer/sink seam where the `SinkFault { reason: "event stream unwritable" }` actually surfaces (T0.6 §5/§6 — the seam this ticket binds against) resolving the code by cause, never by literal; the OS-signal wiring (T36), the fold of a crashed stream into an artifact (T42/T68 — SL3), and the full two-runs test (T67) still layer on top. |
| C20 | machine | — | unmapped | T40 | Produced in an empty environment; byte-identical outside generation time; every node/edge incl. declared resources; validates against the published schema (T40, schema from T39). |
| C21 | human | — | release-checklist | — | "documentation-at-point-of-use (C21)" is a judgment (criterion 8). C21's mechanical sub-criteria are machine-tested under T41; the criterion's governing class is human. |
| C22 | machine | — | unmapped | T42 | Every graph node with propagated states; crashed-run fold marked interrupted; phase durations sum exactly; structural fingerprint matches a same-build graph artifact; validates against schema + prior-version fixtures (T42/T48/T43/T68). |
| C23 | machine | — | unmapped | T44 | New measurement with no framework change; framework metrics present without a task; reserved-prefix attach fails + deterministic cap truncation; documented naming/units convention (T44). |
| C24 | human | — | release-checklist | — | "diagram readability (C24)" is a judgment (criterion 8). C24's mechanical proxies (DOT/Mermaid parse in CI; golden tests) are machine-tested under T46/T47; the criterion's governing class is human. |
| C25 | machine | — | unmapped | T45 | Any attempt log line traceable to node+attempt without timestamp correlation; structured/human switch needs no code change; concurrent-node lines separable; secret sentinel never on framework output paths (T45). |
| C26 | machine | — | unmapped | T55 | Verbs behave identically across pipelines; validate exits non-zero + prints all problems; exit-code table exhaustive; no-args prints verbs cleanly; fold produces the interrupted artifact (T55/T56/T68). |
| C27 | machine | — | unmapped | T57 | Structural mismatch refuses + prints diff; satisfied node not re-executed; undemanded prior success is satisfied-from-prior; dangling durable reference fails; no-op resume of a full success; lineage links parent and root (T57/T58/T59). |
| C28 | machine | — | unmapped | T60 | Sync single-task test needs no runtime; structure test fails on add/remove/rename/rewire/regroup/policy-change and not on rebuild/toolchain-bump; single documented fixture-update flow; fault-injection suite (T60/T61/T62). |

## System-level criteria (criteria 1–8)

| Criterion | Class | Platform | Test | Covered-by | Notes |
|---|---|---|---|---|---|
| SL1 | machine | — | unmapped | T64 | **machine** — README quickstart compiles and runs verbatim in CI, empty directory to an artifact-inspected two-node pipeline (T7/T64). The "under thirty minutes" walkthrough is the **human** facet, audited on the release checklist (recorded under SL8human), not a CI timer. |
| SL2 | machine | — | unmapped | T12 | Mis-wiring two tasks is a compile error whose message contains both type names; UI tests on the pinned toolchain. T8 shipped the pinned-toolchain UI harness (`crates/core/tests/ui.rs`) and the both-type-names assertion; the mis-wiring case against the real authoring API lands in T12, so this stays `unmapped`/deferred to T12. |
| SL3 | machine | — | unmapped | T27 | Every run produces artifacts — normal, crashed (fold verb), cancelled, assembly/bootstrap-failed (T27/T42/T68). |
| SL4a | machine | — | unmapped | T41 | *Structural* determinism — two builds of the same source produce identical fingerprints and byte-identical graph artifacts (generation time aside), on different toolchains (T15/T41/T65). |
| SL4b | machine | — | unmapped | T62 | *Interpretive* determinism — the same recorded outcomes yield the same terminal states, propagation decisions, and artifact, replayed through the C28 harness (T62/T65). |
| SL4c | disclaimer | — | — | — | What tasks *do* against external systems is theirs; the tool claims nothing about it. Carried here **present and unclassified-as-testable** so it is never silently absent (arch.md criterion 4(c)). |
| SL5 | machine | — | unmapped | T43 | A run's duration and resource profile are answerable entirely from artifacts, with no access to the producing machine (T43/T65). |
| SL6 | machine | — | unmapped | T61 | Adding a node requires no changes outside its own module, the assembly site, the structure fixture, and (new resource) registry construction in `main` — structure-diff on a reference pipeline (T61/T65). |
| SL7 | machine | — | unmapped | T65 | Nothing requires a server, database, or scheduler; the binary and its arguments suffice to run and produce local artifacts (T65). |
| SL8machine | machine | — | verifier_passes_against_the_checked_in_matrix | T7 | **Mapped now.** This coverage is verified in CI from this checked-in matrix by [`scripts/check-coverage-matrix.sh`](../scripts/check-coverage-matrix.sh); the mapped test ([`crates/cli/tests/coverage_matrix.rs`](../crates/cli/tests/coverage_matrix.rs)) runs that verifier against the real matrix and suite. T7 builds the enforcement; T65 is the full acceptance gate. |
| SL8human | human | — | release-checklist | — | The human-classed criteria — diagram readability (C24), documentation-at-point-of-use (C21), the thirty-minute walkthrough (SL1's human part), and C1's types-readable-from-the-declaration — are on the version-controlled release checklist, reviewed like code. |

## Build-provenance row (Stability, C20)

Build-provenance verification — embedding tool version, git commit, and lockfile
hash in every binary and artifact (arch.md "Stability": Supply chain) — is not
yet emitted by any built component. It is **not** a separate arch.md acceptance
criterion; it is a facet of **C20** (graph artifact) and the Stability
supply-chain commitment, so it does not get its own criterion row (the verifier
requires exactly the partition's id set). It is tracked here as an explicit
`unmapped` obligation folded into **C20**'s row above (Covered-by T40): when the
graph-artifact component emits provenance, T40 replaces C20's `unmapped` with the
provenance-assertion test id. Recorded here so the obligation is visible now
rather than discovered at the T65 acceptance gate.

## Platform-conditional criteria (for T70)

The rows flagged `platform-conditional` — **C12** (limit detection), **C16**
(signal handling), **C19** (flush/fsync behavior) — remain `machine` (they are
tested), but their pass/fail depends on the platform. T70 attaches the
Linux-tier-1 full-suite and macOS-core-suite jobs and gates these rows per
platform; this matrix names them so that ticket has an unambiguous target.
