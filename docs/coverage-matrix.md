# dagr acceptance-criteria coverage matrix

> **Status:** enforcement artifact, authored by ticket T7 (ticket 006,
> [`docs/implementation/006-T7-ci-pipeline-and-criteria-matrix.md`](implementation/006-T7-ci-pipeline-and-criteria-matrix.md)).
> A **checked-in, review-owned data file** — never a runtime registry, a
> metadata store, or a scheduler input (that boundary is permanent; see
> [`docs/arch.md`](arch.md) "When not to use this"). Edited only by pull
> request, reviewed like code.

This file is the **coverage** half of arch.md system-level acceptance
**criterion 8**: *"every machine-classed acceptance criterion … is covered by an
automated test, and that coverage is itself verified in continuous integration
from a checked-in criteria matrix."* It binds each **machine**-classed criterion
to the **test id** that covers it, or to an explicit **`unmapped`** placeholder
while its covering test has not yet shipped. It is verified in CI by
[`scripts/check-coverage-matrix.sh`](../scripts/check-coverage-matrix.sh).

## Two matrices, one boundary

There are two distinct artifacts; do not conflate them:

- [`docs/criteria-matrix.md`](criteria-matrix.md) — the **PARTITION** (ticket
  T0.10): each arch.md criterion labelled `machine` / `human` / `disclaimer`.
  It is the authoritative classification. **This file does not restate or
  re-decide it**; the verifier reads each criterion's class *from the partition*
  and fails if this file disagrees.
- **This file** — the **COVERAGE matrix**: for every criterion the partition
  lists, a row binding a machine criterion to its test id (or `unmapped`), the
  owning ticket, and the platform-conditional flag T70 keys on.

## How the verifier reads this file

[`scripts/check-coverage-matrix.sh`](../scripts/check-coverage-matrix.sh) parses
the table below and **fails CI** when any of these holds (each is exercised by
the verifier's own self-tests,
[`scripts/check-coverage-verifier-selftest.sh`](../scripts/check-coverage-verifier-selftest.sh)):

- a criterion in the partition is **absent** from this matrix;
- a criterion appears on **more than one** row;
- a **machine** criterion is **`unmapped` and owed by this ticket** (`Covered-by`
  is `T7`, empty, or `—`) — its covering test was supposed to exist already;
- a **machine** criterion maps to a **test id that does not exist** in the
  `cargo test --workspace` suite (a dangling reference).

**`unmapped` is the normal early state.** A machine criterion whose covering
test ships in a *later* ticket is legitimately `unmapped` and names that future
ticket in `Covered-by` (quality-gates.md §3). Every component ticket, as it
lands its covering test, edits this matrix to replace `unmapped` with the test
id — that per-ticket duty is what keeps the debt from exploding at the T65
acceptance gate. Human and disclaimer rows never carry a test.

## Column meanings

| Column | Meaning |
|---|---|
| **Criterion** | The criterion id, exactly as classified in the partition (`C1`–`C28`; `SL1`–`SL7` with criterion 4 split into `SL4a`/`SL4b`/`SL4c`; criterion 8 split into `SL8machine`/`SL8human`). |
| **Class** | `machine` \| `human` \| `disclaimer`, mirroring the partition (the verifier fails on any disagreement). |
| **Platform** | `platform-conditional` for the limit-detection (C12), signal-handling (C16), and flush/fsync (C19) criteria, so T70 can attach the Linux-tier-1 and macOS-core jobs later; `—` otherwise. |
| **Test** | For a machine criterion: the mapped test id (as `cargo test -- --list` prints it) **or** `unmapped`. For human: `release-checklist`. For disclaimer: `—`. |
| **Covered-by** | The ticket that ships (or shipped) the covering test. `T7` means "owed by this ticket"; any later tid means the mapping is deferred to that ticket; `—` for human/disclaimer rows. |
| **Notes** | Free text: what the mapped/owed test asserts. |

## Layer A–D criteria (C1–C28)

| Criterion | Class | Platform | Test | Covered-by | Notes |
|---|---|---|---|---|---|
| C1 | human | — | release-checklist | — | "types readable from the declaration" is a judgment (criterion 8). C1's mechanical sub-criteria are machine-tested under T9/T29/T14 but the criterion's governing class is human. |
| C2 | machine | — | ui | T12 | **Mapped by T12** (`crates/core/tests/ui.rs`, test `ui` — the single T8 UI-harness entry point that runs every `tests/ui/*.rs` compile-fail case under the pinned toolchain). C2's compile-fail acceptance is covered by the real-authoring-API cases T12 ships: cycle inexpressibility over **data** edges (`wiring_data_cycle_self_edge`, `wiring_data_cycle_back_edge`, against `dagr_core::flow::Flow::register` — E0425), over **ordering** edges (the adopted T0.9 `ordering_edge_self_cycle` / `ordering_edge_back_edge`), the unforgeable handle (`wiring_handle_unforgeable`, real `dagr_core::handle::Handle` — E0451 private fields) and the no-by-name/index/string lookup (`wiring_handle_no_lookup`, real `Flow`/`Pipeline` — E0599). The handle copy/pass and rename-changes-identity / reorder-changes-nothing node-identity facets are the compile-PASS positive counterparts asserted by `crates/core/tests/typed_handle_positive.rs`, `crates/core/tests/typed_handle_behavior.rs`, and `crates/core/tests/flow_builder.rs` (T10/T13). No longer `unmapped`. |
| C3 | machine | — | ui | T12 | **Mapped by T12** (`crates/core/tests/ui.rs`, test `ui` — the single T8 UI-harness entry point that runs every `tests/ui/*.rs` compile-fail case under the pinned toolchain). C3's compile-fail acceptance is covered by the real-authoring-API (`dagr_core::binding`) cases: wrong-type binding naming BOTH type names (`data_binding_wrong_type` — E0271, `Alpha`+`Beta`; its both-names assertion is proven non-vacuous by the harness's substring contract), wrong arity too-few/too-many (`data_binding_wrong_arity_too_few`/`_too_many` — E0271), the arity-ceiling curated diagnostic at the documented ceiling of 8 (`data_binding_arity_ceiling` — the `#[diagnostic::on_unimplemented]` message, not a trait-error wall), the non-`all-succeeded` trigger-rule typestate on a data-dependent node (`data_binding_non_default_rule` — E0599, the `ConsumesData` state offers no `trigger_rule` method), and the type-level ownership slice (`wiring_owned_receive_not_deliverable` — an owned receive of a `!Send` value fails to compile; the whole-graph multi-consumer conflict is an assembly error deferred to T14). The `all-succeeded` positive counterpart and one-handle-fans-out are in `crates/core/tests/data_binding_positive.rs`. No longer `unmapped`. |
| C4 | machine | — | unmapped | T50 | Registration-time backward-reference edge; both edge kinds recorded; effect-only node has no value; propagation across ordering edges (T50). |
| C5 | machine | — | all_defaults_node_equals_no_policy_under_the_policy_hash | T29 | **Mapped by T29 (039)** — the full C5 node-policy surface lands in `dagr_core::assembly` (`NodePolicy`, resolved `EffectivePolicy`) against the real `Flow`/`Pipeline`/`assemble` path, one arch.md acceptance criterion per test in `crates/core/tests/node_policy.rs`. The mapped headline test (`all_defaults_node_equals_no_policy_under_the_policy_hash`) covers arch.md C5's *"A node with no stated policy behaves identically to one with every default written out — including under the fingerprint's policy hash (C21)"* (structural fingerprints match too); its siblings cover the rest of C5's owned surface: **every field's documented default applied uniformly** → `every_field_has_its_documented_default`; **all-defaults == no-policy field-for-field** → `all_defaults_node_equals_no_policy_node`; **full effective policy incl. defaults reaches the artifact** → `full_effective_policy_including_defaults_is_present`; **a single policy value moves only the policy hash** → `a_single_changed_policy_value_changes_only_the_policy_hash`; **per-attempt timeout is a policy value** → `timeout_feeds_the_policy_hash_not_the_structural_fingerprint`; **trigger rule feeds the structural fingerprint, not the policy hash** → `trigger_rule_feeds_the_structural_fingerprint_not_the_policy_hash`; **group in neither hash** → `group_is_in_neither_hash`; **valid execution-class override on synchronous work assembles** → `valid_execution_class_override_on_synchronous_work_assembles`; **invalid override on await-bound work fails assembly, names the node, and does not short-circuit T14's all-problems reporting** → `invalid_execution_class_override_fails_and_does_not_short_circuit`; **declared cost vector per-pool native units with the working/output-residency memory split** → `declared_cost_vector_carries_native_units_with_the_memory_split`; **policy attached at registration with no task-code change** → `setting_a_policy_value_requires_no_task_code_change`; **retention flag is policy and feeds the policy hash** → `retention_flag_is_policy_and_feeds_the_policy_hash`; **durability flag lives in policy and arms the existing assembly durable-without-contract check** → `durability_flag_arms_the_assembly_contract_check`; **the migrated retry+backoff shape has one home and produces the runner's `RetryConfig`** → `policy_owns_retries_and_backoff_and_produces_the_runner_config`. The invalid-execution-class-override assembly facet is also covered by T14's `crates/core/tests/assembly.rs` (`invalid_execution_class_override_fails_assembly`), which stays green over the expanded policy. **Deferred (Out of scope):** runtime consumption of these knobs — admission/capacity against the cost vector (T31/C12), execution-class dispatch (T33/C13), and trigger-rule runtime (T34/C15) — and the concrete BLAKE3 hash algorithm + graph-artifact schema (T41/T40/C21) are downstream; T29 defines only the values they read. No longer `unmapped`. |
| C6 | machine | — | unmapped | T51 | Unique names across grouping; diagram clusters by group; group rename changes no execution behaviour and no fingerprint. **The diagram-clustering facet landed with T46 (057)** — `crates/render/tests/renderer.rs::groups_render_as_clusters_in_dot` and `::groups_render_as_clusters_in_mermaid` prove each group renders as a cluster containing exactly its members, ungrouped nodes sit outside every cluster, and groups do not nest (arch.md C6 line 170), rendered from a real published graph artifact by `dagr_render`. The **headline** C6 mapping (unique names across grouping; group excluded from identity and both fingerprints so a rename is review-visible but never breaks resume) is owned by **T51 (024)**, which consumes the same group label this ticket clusters on; C6 stays `unmapped` deferred to T51 rather than re-pointing the headline `Test` cell at the T46 clustering facet, since the fingerprint-exclusion half is C6's load-bearing acceptance and is not T46's to assert. |
| C7 | machine | — | assembling_twice_yields_byte_identical_graph_artifacts | T14 | **Mapped by T14 (025)** — the assembly-validation-and-precomputation ticket ships the C7 machine acceptance criteria T13 deferred, in `crates/core/tests/assembly.rs` against the real assembly pass `dagr_core::flow::Pipeline::assemble` (`dagr_core::assembly`). The mapped headline test (`assembling_twice_yields_byte_identical_graph_artifacts`) covers arch.md C7's *"assembling the same pipeline twice … produces byte-identical graph artifacts"* (generation-time field aside); its siblings in the same suite cover the rest of C7's owned surface, one arch.md acceptance criterion each: **duplicate-name failure naming both declarations** → `duplicate_node_name_names_both_declarations`; **empty-environment assembly (assembly succeeds with every external resource absent)** → `assembly_is_pure_runs_in_an_empty_environment`; **all-problems-reported-not-just-the-first** → `all_problems_are_reported_not_just_the_first` (+ `fixing_one_defect_still_surfaces_the_rest`); **exact consumer counts before execution** → `consumer_counts_are_exact_before_execution`; **no-parameter-reachable** → a *structural* fact (the `AssemblyArtifact` surface exposes no parameter accessor — see the module rustdoc), demonstrated by `assembly_is_pure_runs_in_an_empty_environment` assembling with no parameters present. Further assembly checks land as their own tests: empty-pipeline (`empty_pipeline_is_rejected`), invalid class override (`invalid_execution_class_override_fails_assembly`), durable-without-contract (`durable_node_without_the_contract_fails`), ownership-mode conflicts (`owned_demand_on_a_multi_consumer_value_fails`, `owned_edge_into_a_retrying_node_without_clone_on_read_fails`), nonzero teardown cost (`nonzero_teardown_cost_fails`), the zero-consumer warning (`zero_consumer_non_unit_output_is_a_warning_not_an_error`), remaining-dependency counts (`remaining_dependency_counts_match_the_graph`), topological order (`execution_order_is_a_valid_topological_order`), and the empty-by-default env-capture allowlist (`environment_capture_allowlist_is_empty_by_default`). **Still deferred to T15:** the C7 acceptance line *"Bootstrap rejects, before any node executes: a declared cost no pool can ever satisfy, a missing declared resource, an invalid parameter"* is a **bootstrap** check (T0.5 ADR §5/§7 moved cost-fit off assembly), not assembly's — T14 deliberately makes no capacity/cost-fit check, so that facet stays owned by the bootstrap-runtime ticket (T15/T24/T29) and is not covered here; the single `Test` cell maps the assembly facet T14 owns while the note records the deferred bootstrap facet. **T13 (023)** shipped the flow builder and node identity (`dagr_core::flow`), which this ticket validates. The rename-changes-identity / reorder-changes-nothing *node-identity* facet remains owned by **T12**'s wiring compile-fail suite (see the C2 row). **Determinism/purity regression guards added by T15 (026)** — additive, not re-mapping the `Test` cell (which stays T14's headline test): `crates/core/tests/determinism_and_purity.rs` pins the C7 determinism and purity acceptance criteria against the same real assembly pass, one facet per test — byte-identical twice, generation-time masked (`two_in_process_assemblies_are_byte_identical`); generation-time is the sole (empty) mask (`generation_time_is_the_only_masked_difference`); registration order does not change the artifact (`registration_order_does_not_change_the_artifact`); consumer counts exact per node + all precomputed runtime data identical across two assemblies (`precomputed_runtime_data_is_identical_across_assemblies`); assembly succeeds in an empty environment via a scrubbed child process (`assembly_succeeds_in_an_empty_environment`) with a non-vacuous negative control proving the empty-CWD guard bites (`negative_control_stray_filesystem_write_is_detected`); no parameter value reachable during assembly (`no_parameter_value_is_reachable_during_assembly`); and determinism-and-purity combined under the scrub — the C20 CI/PR condition (`empty_environment_determinism_combined`). The mechanical no-filesystem/no-network proof convention (std-only scrubbed-child-process + negative control) is recorded in ticket 026's decision record and reused by T40 / the criteria-matrix structural-determinism job. |
| C8 | machine | — | all_fields_populated_on_a_hand_built_context | T16 | **Mapped by T16** (`crates/core/tests/run_context.rs`): a hand-built `RunContext` populates *every* field on the first attempt and each accessor returns exactly the supplied value — the headline C8 acceptance ("every field populated on every invocation, including the first attempt of the first node"). Sibling tests in the same file cover the rest of C8's owned surface: attempt/max readable (`attempt_number_reflects_the_supplied_attempt`), data interval recorded verbatim (`data_interval_is_carried_verbatim_and_never_interpreted`), and the no-mutation/no-scheduling API shape (`context_exposes_no_mutation_or_scheduling_authority`). The remaining acceptance facet — the attempt number *incrementing across retries* and *appearing in logs and artifacts* — is out of T16's scope (T16 only exposes the field); it is demonstrated end-to-end by the retry runner (T22) and node metrics/artifacts (C22/C23), which map their own rows. |
| C9 | machine | — | a_missing_declared_resource_fails_bootstrap_naming_the_resource_and_nodes | T30 | **Mapped by T30** (`crates/core/tests/resource_registry.rs`, headline test `a_missing_declared_resource_fails_bootstrap_naming_the_resource_and_nodes`) against the real registry `dagr_core::context::ResourceRegistry` and `ResourceRegistry::validate_requirements`, covering arch.md C9's *"a run whose pipeline declares an unregistered resource fails at bootstrap, before any node executes, naming both the missing resource and the nodes requiring it"* — asserting the named missing resource type and the **exact set** of requiring node ids. The sibling facets in the same suite each cover one arch.md C9 acceptance line: **bootstrap-failure artifact produced, distinct from assembly failure, with zero attempts** → `a_missing_resource_produces_the_bootstrap_failure_artifact_with_zero_attempts`; **all-requirements-satisfied passes** → `all_requirements_satisfied_passes_validation`; **registry immutable / shared read-only in-run** → `a_built_registry_is_shared_read_only`; **any resource replaceable by a fake with no task change** → `a_fake_resource_substitutes_with_no_task_change` (with `a_task_reads_a_resource_through_the_run_context` proving the task reaches it through the C8 context); **two same-typed resources distinguished by newtype** → `newtype_wrappers_disambiguate_two_same_typed_resources`; **ambiguous duplicate rejected, first not replaced** → `duplicate_same_type_registration_is_rejected_as_ambiguous` (+ `a_rejected_duplicate_does_not_replace_the_first`); **type-keyed retrieve/miss** → `retrieve_by_type_returns_the_registered_resource` / `retrieving_an_unregistered_type_yields_none`; **declared requirements surfaced for the graph artifact** → `declared_requirements_are_surfaced_for_artifact_emission`; **marked secret never appears on a framework-controlled path (planted sentinel)** → `a_secret_sentinel_never_appears_in_a_framework_emission_path`. The **compile-time** guarantees are the T8 UI harness's (`crates/core/tests/ui.rs`, test `ui`): the secret wrapper's absent `Debug`/`Display` path (`tests/ui/secret_no_debug.rs`, `tests/ui/secret_no_display.rs` — E0277) and the `Send + Sync + 'static` stored-resource bound (`tests/ui/registry_non_send_resource.rs` — E0277). **Deferred beyond T30:** end-to-end framework-emitted log-line redaction is C25/T45 (this ticket ships the wrapper + sentinel hook only); rendering the graph/bootstrap-failure artifact is C20/C22 (this ticket produces the outcome value, `dagr_core::context::BootstrapFailure`, and asserts it is produced). No longer `unmapped`. |
| C10 | machine | — | release_fires_only_after_every_consumer_terminal_and_returned | T17 | **Mapped by T17 (027)** — the output-slot substrate ships C10's owned machine acceptance in `crates/core/tests/output_slot.rs` against the real slot API (`dagr_core::slot`: `Slot`/`SlotRef`/`ConsumerLease`/`ResidencyLedger`/`RedemptionHandle`). The mapped headline test (`release_fires_only_after_every_consumer_terminal_and_returned`) covers arch.md C10's load-bearing release rule — *"after the final consumer reaches a terminal state and every consumer's work has returned, that node's value is unreachable and its memory returned to the allocator"* — and its `release_waits_on_closure_return_not_terminal_decision` / `zombie_consumer_pins_value_and_residency` siblings pin the zombie-aware half (*"while an abandoned consumer's closure is still running, the residency stays counted"*). Siblings in the same suite cover the rest of C10's owned surface, one acceptance criterion each: **never-read-before-fill loud node-named defect** → `read_before_fill_is_a_loud_node_named_defect`; **once-writable** → `second_fill_is_rejected_original_unchanged` (+ `fill_once_then_read_the_value`); **shared retry finds the value intact** → `shared_consumer_retry_finds_the_value_intact`; **retained redeemable / released not redeemable** → `retained_value_survives_to_run_end_and_is_redeemable`, `released_value_is_not_redeemable`, `redeeming_an_unfilled_slot_reports_never_filled_not_released`; **single-count residency** → `residency_is_counted_once_not_per_consumer`; **peak measured slot residency (accounting hook)** → `accounting_hooks_expose_peak_residency`; **no-lookup/no-type-check read path** → `no_lookup_no_type_check_on_the_read_path`; the three T0.2 delivery modes → `owned_delivery_moves_the_value_out`, `clone_on_read_gives_each_attempt_a_fresh_value`. **Bounded-memory facet added by T26 (036)** — additive, not re-mapping the `Test` cell (which stays T17's release-rule headline): `crates/cli/tests/bounded_memory_chain.rs` ships C10's authoritative *peak-memory-flat over a **hundred-node** chain* assertion (*"Peak allocator-level memory across a long chain does not grow with the chain's length when nothing is retained — verified against a synthetic hundred-node chain"*), driving the real merged pieces — real T17 output slots (`Slot`/`SlotRef`/`ConsumerLease`/`ResidencyLedger`/`RedemptionHandle`) run to completion through the **real** T24 driver (`dagr_cli::driver::drive`) on a shared ledger. Two allocator-level instruments, both deterministic and never RSS: the exact per-run `ResidencyLedger` peak (the load-bearing integer) and a test-only `#[global_allocator]` recording live/peak allocated bytes. One arch.md facet per test: **peak is flat across chain length (short vs hundred-node)** → `ledger_peak_is_flat_across_chain_length` (+ the allocator-level restatement `allocator_peak_is_flat_across_chain_length`); **peak bounded by a few concurrent values, not the whole chain** → `ledger_peak_bounded_by_a_few_values_not_the_whole_chain`; **value released after the sole consumer is terminal-and-returned** → `value_released_after_sole_consumer_terminal_and_returned`; **retained survives & is redeemable while released ones are not** → `retained_value_survives_and_is_redeemable_released_ones_are_not`; **determinism / no flakiness** → `ledger_peak_is_deterministic_across_repetitions`; and a non-vacuity guard proving a chain with release defeated peaks proportionally to length → `peak_grows_with_length_when_release_is_defeated`. T17's smaller in-crate smoke (`chain_peak_does_not_grow_with_chain_length`) still guards the release logic in `crates/core`. **Zombie-consumer residency cross-check added by T37 (047)** — additive, not re-mapping the `Test` cell (which stays T17's release-rule headline): `crates/core/tests/permit_release_matrix.rs`'s `a_zombie_consumer_pins_slot_residency_until_its_closure_returns` composes the C10 `ResidencyLedger` slot lease with the C12 ledger to assert *"while an abandoned consumer's closure is still running, the residency stays counted against the memory pool … only after the consumer closure returns is the value's residency reclaimed to the allocator"* — the C10↔C12 honesty rule from the T37 permit-release matrix (the memory pool never regains capacity for bytes a leftover thread still pins). **Deferred to C23 (T44):** *measured peak slot residency appearing in the run artifact* — T17 exposes, and T26 asserts, the `ResidencyLedger` peak hook the artifact folds, but rendering that number into the run-artifact summary is C23's. The single `Test` cell maps the release-rule facet T17 owns while the note records the T26-landed bounded-memory facet and the deferred C23 facet. |
| C11 | machine | — | diamond_proves_no_wave_batching | T18 | **Mapped by T18 (028)** — the readiness tracker ships C11's owned machine acceptance in `crates/core/tests/readiness.rs` against the real tracker (`dagr_core::readiness`: `ReadinessTracker`/`Decision`/`evaluate_rule`/`RuleOutcome`) driving synthetic pipelines with injected terminal outcomes (no runtime, no clock). The mapped headline test (`diamond_proves_no_wave_batching`) covers C11's load-bearing acceptance — *"in a diamond with one slow branch, the fast branch's descendants are not delayed by the slow branch unless they depend on it"* and *"work is never batched into waves"* — the fast branch's independent descendant is emitted ready before the slow branch reaches any terminal state and while the join stays not-ready. Siblings in the same suite cover the rest of C11's owned surface, one acceptance criterion each: **countdown seeds from T14's precomputed counts / source frontier** → `countdown_seeds_from_precomputed_dependency_counts`, `source_nodes_are_ready_without_any_notification`; **decrement-on-terminal unlocks exact dependents** → `decrement_on_terminal_unlocks_the_exact_dependents`; **all-upstreams-terminal evaluation gate (no partial fire)** → `rule_is_not_evaluated_on_a_partial_result`; **`all-succeeded` fires on the last upstream** → `all_succeeded_fires_when_the_last_upstream_completes`; **can-never-fire → propagated terminal per the T0.4 §5a table, carrying the originating node** → `all_succeeded_can_never_fire_upstream_failed`, `all_succeeded_can_never_fire_upstream_skipped`, `all_succeeded_can_never_fire_cancelled`, `mixed_non_success_classes_propagate_upstream_failed`, `propagated_upstream_skipped_counts_skip_like`; **`satisfied-from-prior` counts success-like** → `satisfied_from_prior_counts_success_like`; **propagated-terminal cascade without intervening execution** → `propagated_terminal_cascades`; **exactly-one-terminal-state / no double assignment** → `every_node_ends_in_exactly_one_terminal_state`, `a_decided_node_is_not_assigned_a_terminal_state_twice`; **pending accounting reaches zero at run end** → `pending_accounting_reports_run_completion`; and the **full three-rule seam (`all-succeeded`/`all-terminal`/`any-failed`) present though only `all-succeeded` fires in M1** → `evaluate_rule_all_succeeded_matches_the_table`, `evaluate_rule_all_terminal_always_fires`, `evaluate_rule_any_failed_matches_the_table`. **Now mapped by T25 (035)** (was "deferred to T25"): the randomized-DAG, randomized-outcome **deadlock-freedom property test** (arch.md C11 *"a test over randomly generated graphs with randomized outcomes confirms every run terminates"*) ships in T25 — see the additive T25 paragraph below. The **`all-terminal` / `any-failed` runtime firing** on runtime nodes (stop-mode contingency admission and the like) is C15/T34, which reuses the same `evaluate_rule` seam; T18 wires only `all-succeeded` onto runtime nodes. The single `Test` cell maps the no-wave-batching facet T18 owns while the note records the T25-landed termination facet and the deferred T34 facet. **Run-loop-driven facet added by T24 (034)** — additive, not re-mapping the `Test` cell (which stays T18's headline test): `crates/cli/tests/run_loop_driver.rs` drives the *real* tracker through the M1 run-loop driver (`dagr_cli::driver::drive`) end-to-end, proving C11's readiness half at runtime rather than against injected outcomes — the no-wave-batching guarantee driven for real (`fast_branch_not_gated_on_slow_branch`: a diamond whose fast-branch descendant is admitted before the slow branch terminates), decrement-and-admit on individual completion (`linear_chain_drives_dependents`, `every_outcome_is_fed_back`: each dependent's `node-ready` follows only its upstream's `node-terminal`), propagated-terminal-without-execution driven for real (`failing_upstream_propagates_and_run_fails`: the deadened downstream ends `upstream-failed` and emits no `attempt-started`), and precise termination when nothing is pending or in flight (`run_ends_when_nothing_pending_or_in_flight`). **Termination-property facet added by T25 (035)** — additive, not re-mapping the `Test` cell (which stays T18's headline test): `crates/core/tests/termination_property.rs` ships C11's anti-deadlock acceptance line — *"The tracker cannot deadlock: a test over randomly generated graphs with randomized outcomes confirms every run terminates."* It drives the *real* `ReadinessTracker` (the crux state machine) over thousands of hand-generated valid acyclic DAGs (bounded node count, acyclic lower→higher edges, mixed fan-in arities to the C3 ceiling, `all-succeeded` runtime nodes) with randomized per-node outcomes (success, permanent failure, deliberate skip, timeout, retry-then-succeed→success), mirroring the T24 admit→feed-back→admit loop, and asserts all four invariants as emergent properties over random shapes: **termination within a bounded step budget** — a budget exhaustion is treated as a deadlock and fails the property (`every_generated_run_terminates_and_holds_the_invariants`); **exactly one normative-taxonomy terminal state per node** (never off-taxonomy, never `not-requested`); **the run-boundary property** — nothing pending at run-finished and no node terminating after it; and **propagation consistency** — never-ran propagated nodes (`upstream-skipped`/`upstream-failed`) are never executed, executed-class nodes run exactly once. Pinned regression cases: the mixed-rule diamond (`regression_mixed_rule_diamond`, with the `all-terminal`-fires-downstream-of-failure counterpart at the `evaluate_rule` seam), the all-skips-succeeds graph (`regression_all_skips_graph`), and recorded-seed replay proving byte-for-byte reproducibility (`replay_recorded_seed`); an opt-in meta-test (`shrinking_produces_a_minimal_counterexample`, `#[ignore]`) documents that the property bites and the shrinker minimises. The suite uses a hand-rolled dependency-free deterministic seeded generator (the ticket's "or equivalent" to proptest — no new dependency, audit/deny untouched); the case count is env-tunable (`DAGR_TERMINATION_CASES`, higher in CI than local) and every case is seed-replayable. A companion driver-level check `crates/cli/tests/termination_property_driver.rs` (`generated_runs_terminate_through_the_real_driver`) drives a small fixed set of the same generated DAGs through the *real* T24 `dagr_cli::driver::drive` against fake scripted runners, proving the full two-runtime run loop terminates too. The `all-terminal`/`any-failed` *runtime firing* over random shapes stays deferred to T34. **Non-default-rule runtime-firing facet added by T34 (044)** — additive, not re-mapping the `Test` cell (which stays T18's headline test): the `all-terminal` / `any-failed` runtime firing that T18 left to T34 (the "deferred T34 facet" above) now ships against the *real* tracker and the *real* driver. `crates/core/tests/readiness.rs` drives the pure `ReadinessTracker` over the run-level ordering seam (`new_with_ordering`) so a consume-nothing non-default-rule node acquires the upstreams its rule fires against — `any_failed_contingency_fires_on_a_failed_ordering_upstream`, `any_failed_contingency_never_arose_is_skipped`, `all_terminal_cleanup_fires_after_an_upstream_failure`, `any_failed_fires_on_a_transitive_upstream_failed`, with `empty_ordering_map_matches_the_default_constructor` proving the seam is purely additive — and `crates/cli/tests/failure_policy_and_propagation.rs` drives the same firing through the real T24/T34 run loop (see the C15 row). The tracker's `evaluate_rule` seam is unchanged; T34 only wires the node's own rule through readiness → the driver and adds the ordering seam. |
| C12 | machine | platform-conditional | admission_pools_hold_capacity_and_permits_release_without_leaking | T31 | **Mapped by T31 (041)** — the runtime admission controller lands in `dagr_core::admission` (`AdmissionController`, `Pool`/`PoolCapacities`/`PoolCost`, the `Permit`/`ResidencyLease` guards, the `zombie_report` seam) against weighted memory/thread pools, one arch.md/T0.3-ADR facet per test in `crates/core/tests/admission_pools.rs`, and is wired into the real T24 run loop (`crates/cli/tests/admission_driver.rs`). The mapped headline test (`admission_pools_hold_capacity_and_permits_release_without_leaking`) drives a whole run through every terminal outcome and asserts every pool ends back at full with zero live zombies — C12's **no-leak / capacity invariant**. Its siblings cover the rest of C12's owned surface: **all-or-nothing multi-pool fit, no partial hold** → `a_node_is_admitted_only_when_it_fits_every_pool_and_holds_no_partial`; **atomic acquisition, two contending nodes, no deadlock** → `two_contending_multi_pool_nodes_make_progress_without_deadlock`; **combined counted cost ≤ capacity incl. a live zombie** → `combined_counted_cost_never_exceeds_capacity_including_a_live_zombie`; **oldest-ready-first, no starvation of a large node** → `a_large_node_behind_a_stream_of_small_nodes_is_admitted_without_starvation`; **bounded bypass never delays the oldest waiter** → `bounded_bypass_never_delays_the_oldest_waiter` (+ `bounded_bypass_admits_a_small_node_when_it_cannot_delay_the_oldest`); **permit releases on success / permanent failure / retry-eligible failure / cooperative cancellation** → `permit_releases_on_success`, `permit_releases_on_permanent_failure`, `permit_releases_on_retry_eligible_failure_and_next_attempt_reacquires`, `permit_releases_on_cooperative_cancellation`; **timed-out blocking permit held until the closure returns** → `permit_for_a_timed_out_blocking_attempt_is_held_until_the_closure_returns`; **working-memory vs output-residency split + the slot lease held to actual slot release / run end** → `working_memory_and_output_residency_are_charged_separately`, `slot_lease_is_held_until_the_slot_actually_releases`, `a_retained_output_is_charged_until_run_end`; **permit-wait recorded separately from execution** → `permit_wait_time_is_recorded_separately_from_execution_time`; **undeclared-cost warning only in a memory-constrained run** → `an_undeclared_cost_node_warns_in_a_memory_constrained_run`; **declared per-pool + zombie cost surfaced for the T42/C23 juxtaposition** → `declared_cost_is_exposed_for_the_artifact_juxtaposition`; **ledger isolated from a misbehaving task** → `the_ledger_is_isolated_and_cannot_be_corrupted_into_over_crediting`; **driver integration (a pinned pool admits one node at a time, the run still completes)** → `a_pinned_pool_admits_one_node_at_a_time_and_the_run_still_completes`. **Container-limit sizing facet added by T32 (042)** — the bootstrap probe that derives the default `PoolCapacities` from the machine's actual limits lands in `dagr_core::limits` (`ContainerLimitProbe`, `PinnedPools`, `detect_capacities`, the `CapacityBootstrapFailure`/`CapacityError` artifact), tested against an **injected fixture cgroup/proc tree** (never the real host — deterministic on macOS + Linux CI) in `crates/core/tests/container_limits.rs`, and wired into the real T24 driver (`crates/cli/tests/container_limits_driver.rs`). This is the platform-conditional half of C12 (Tier-1 Linux cgroups vs macOS host fallback; gated by T70). Facets: **cgroup v2 preferred → v1 → host precedence** → `container_limits_size_the_admission_pools_from_cgroup_v2`, `cgroup_v1_is_used_when_v2_is_absent`, `host_resources_are_used_when_no_cgroup_exists`; **per-dimension unlimited-sentinel fallback to host** → `an_unlimited_sentinel_falls_back_to_host_per_dimension`; **20% headroom default** → `the_headroom_fraction_defaults_to_twenty_percent`; **at-least-one-unit floor under a fractional quota** → `every_pool_gets_at_least_one_unit_under_a_fractional_quota`; **the `dagr.`-reserved pinning flag overrides both cgroup detection and host fallback** → `the_pinning_flag_overrides_cgroup_detection`, `the_pinning_flag_overrides_host_fallback`, `pinning_keys_are_in_the_reserved_namespace`; **malformed cgroup values fall back rather than panicking** → `malformed_cgroup_values_fall_back_to_host`; **too-big node rejected at bootstrap (not admission), producing the `bootstrap-failed` artifact with the complete error list and zero attempts** → `a_too_big_node_is_rejected_at_bootstrap_not_at_admission`, `the_too_big_rejection_produces_the_bootstrap_failure_artifact`, `bootstrap_reports_every_too_big_node`, `a_node_exactly_at_capacity_is_admitted_not_rejected`, and the driver integration `a_too_big_node_fails_the_run_at_bootstrap_before_any_node_executes` (`RunOutcome::BootstrapFailed`, distinct from `assembly-failed`, no attempt runs). **Permit-release outcome-matrix facet added by T37 (047)** — additive, not re-mapping the `Test` cell (which stays T31's `admission_pools_hold_capacity_and_permits_release_without_leaking` headline): `crates/core/tests/permit_release_matrix.rs` drives the merged ledger + timeout/residency seams (`AdmissionController` / `Permit`-`Drop` release / `mark_zombie` mark-and-hold / `ResidencyLease` / `all_pools_full` / `zombie_report`, composed with the real T21 `TimeoutDecision` / `LateResultBarrier` / `ZombieObserver` and the T23/T35 `AttemptOutcome` taxonomy) through the **full** outcome × execution-class grid T31 deferred, deterministic by counts + explicit gates (no sleeps/wall-clock/network). Facets, one arch.md/T0.3 rule per test: the **whole-matrix no-leak headline** (every class × outcome, pool restored per the cell's rule, run ends all-pools-full, zero live zombies) → `permit_release_outcome_matrix_leaks_nothing_across_every_class_and_outcome`; **success / permanent-failure / retry-eligible / panic / cooperative-cancellation release immediately for every class** → `success_releases_the_permit_immediately_for_every_class`, `permanent_failure_releases_immediately_and_is_not_retried`, `retry_eligible_failure_releases_between_attempts_and_never_doubly_holds`, `panic_releases_the_permit_immediately_and_the_rest_of_the_run_proceeds`, `cooperative_cancellation_releases_the_permit_immediately`; **the honest exception — a blocking/compute timeout is decided `timed-out` immediately yet HELD until the closure returns, then releases, never a second terminal** → `blocking_and_compute_timeout_hold_the_permit_until_return_and_stay_timed_out`; **a blocking-timeout retry is deferred until the previous closure returns (C1 exclusivity, ledger never counts two live permits for the node)** → `blocking_timeout_retry_is_deferred_until_the_closure_returns`; **abandonment (blocking/compute only) HOLDS the permit until the closure returns** → `abandonment_holds_the_permit_until_the_closure_returns`; **the C12 capacity invariant holds across a whole saturated run incl. a live zombie** → `capacity_invariant_holds_across_a_whole_saturated_matrix_run`; and **exactly one attempt-outcome record per attempt across the matrix incl. the timed-out case** → `every_outcome_emits_exactly_one_attempt_outcome_record`. The **C10 cross-check** — a slot value pinned by a zombie consumer stays counted against the memory pool until the consumer's closure returns, then reclaimed — is `a_zombie_consumer_pins_slot_residency_until_its_closure_returns` (also folded into the C10 row's note). Non-vacuity proven by neutering `Inner::release` (11/12 tests red on the no-leak assertion) then reverting. **Deferred (Out of scope):** the event/artifact fold of declared-vs-measured cost (T42/C23), and the platform-matrix CI wiring (T70). No longer `unmapped`. |
| C13 | machine | — | long_blocking_task_does_not_delay_unrelated_await_bound_work | T33 | **Mapped by T33 (043)** — execution-class **dispatch** lands in `dagr_cli` (the `dispatch::Dispatcher` routing each dispatched attempt onto the surface named by its resolved [`ExecutionClass`](dagr_core::task::ExecutionClass), plus the public `driver::current_execution_surface` / `ExecutionSurface` probe), wired into the **real** T24 run loop (`dagr_cli::driver::drive`), tested end-to-end in `crates/cli/tests/execution_class_dispatch.rs`. Routing is by the T2 ADR (004): await-bound → the async tokio task runtime; blocking → tokio's dedicated blocking pool (`spawn_blocking`); compute → a dedicated fixed-size `rayon::ThreadPool` sized to the pinned `compute_threads` (floor of one). Deterministic in CI: class/counts control, a surface probe observes (no wall-clock/network). The mapped headline test (`long_blocking_task_does_not_delay_unrelated_await_bound_work`) covers C13's first acceptance criterion — *"A long synchronous task does not delay progress on unrelated await-bound work"* (the await-bound node completes while the blocking node is still spinning). Siblings in the same suite cover the rest of C13's owned surface, one acceptance facet each: **await-bound work runs on the async runtime** → `await_bound_node_runs_on_the_async_runtime`; **blocking work runs on the dedicated blocking pool while the async runtime makes progress** → `blocking_node_runs_on_the_blocking_pool_and_async_makes_progress`; **compute-bound work runs on the fixed compute pool** → `compute_node_runs_on_the_compute_pool`; **the C5 policy override wins over the task default (a legal Blocking→Compute move routes onto compute)** → `policy_override_moves_a_blocking_task_onto_the_compute_pool`; **an illegal await-bound→synchronous override never reaches dispatch (assembly rejects it, naming the node — relies on T29)** → `illegal_await_bound_to_synchronous_override_fails_assembly`; **concurrently executing compute-class attempts never exceed the compute pool's size (pinned for CI determinism)** → `compute_pool_concurrency_is_bounded_by_pool_size` (with the tight-bound counterpart `compute_pool_admits_up_to_pool_size_concurrently` proving N nodes on an N-pinned pool all run at once); **the compute pool keeps a floor of one thread under a zero/fractional pinned capacity (T2 §3)** → `compute_pool_has_a_floor_of_one_thread`; **misdeclaring a class may stall progress but never disables the safety rails — a timeout still fires and a complete stream is written while every task worker is blocked** → `safety_machinery_survives_a_fully_blocked_task_fleet` (the isolated framework runtime is not one of the three task surfaces). **Deferred (Out of scope):** the SIGTERM-complete-stream half of C13's third acceptance is proven via the shared isolation the timeout test exercises here; the signal-handling implementation itself is C16 (T35/T36, platform-conditional under T70), and the exhaustive permit-release outcome matrix is T37 — this ticket consumes T31's permit mechanics unchanged and does not re-derive T32 sizing. No longer `unmapped`. |
| C14 | machine | — | successful_attempt_fills_the_slot | T20 | **Mapped by T20 (030)** — the single-attempt execution core ships C14's *single-attempt* machine acceptance in `crates/core/tests/single_attempt.rs` against the real runner `dagr_core::execution::run_attempt` (with `AttemptOutcome` / `AttemptEvent` / `AttemptEventSink`), driven on a hand-built C8 `RunContext` and a capturing C19-shaped sink with no runtime. The mapped headline test (`successful_attempt_fills_the_slot`) covers the load-bearing path — run one attempt, classify success, fill the once-writable output slot (C10), and emit `attempt-started` then `attempt-succeeded`. Siblings in the same suite cover the rest of the T20-owned surface, one acceptance facet each: **exactly one attempt-outcome record per attempt including failures/skips** → `exactly_one_attempt_outcome_record_on_success` + `every_outcome_yields_exactly_one_attempt_outcome_record`; **a permanent error is never retry-eligible regardless of remaining attempts** → `permanent_failure_is_not_retry_eligible_even_with_attempts_remaining` (+ `permanent_failure_does_not_fill_the_slot`); **a retry-eligible error is classified distinctly so T22 can act** → `retry_eligible_failure_is_classified_distinctly`; **a deliberate skip is an originated skip, distinct from success/failure** → `deliberate_skip_is_classified_as_originated_skip`; **non-success never fills the slot** → the failure/skip tests above; **attempt number/max carried through span + events + outcome, and the first attempt carries one** → `attempt_number_is_carried_through_identity` + `first_attempt_carries_attempt_one`; **the span opens before the work runs** → `the_span_opens_before_the_work_runs`; **ordered per-transition events with gapless sequence + node-terminal carries the classified state** → `event_ordering_within_an_attempt` + `node_terminal_record_carries_the_classified_state`; **the value type flows unchanged into the slot (C10)** → `value_type_flows_unchanged`; **runnable in isolation with no runtime** → `runnable_in_isolation_with_no_runtime`. **Per-attempt timeout facet added by T21 (031)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/attempt_timeout.rs` pins C14's per-class timeout acceptance against the real T21 API (`dagr_core::execution::run_attempt_with_timeout` for the await-bound race + `TimeoutDecision` / `LateResultBarrier` / `ZombieObserver` for the blocking/compute mark), runtime-agnostic (the attempt future raced against a caller-provided deadline future, no tokio), one arch.md acceptance facet per test: **await-bound over its timeout → cancelled and permit released immediately** → `await_bound_timeout_cancels_immediately_and_releases_permit`; **blocking over its timeout → recorded timed-out immediately, permit held until the closure returns** → `blocking_timeout_marks_immediately_and_holds_permit_until_return` (+ `compute_timeout_behaves_identically_to_blocking` for the class-shape-driven identical compute case); **retry starts only after the closure returns (C1 exclusivity)** → `retry_of_a_timed_out_blocking_node_is_deferred_past_zombie_return`; **an abandoned attempt's late result never fills a slot and never writes scratch** → `late_result_never_fills_the_slot` + `late_result_never_writes_scratch`; **exactly one attempt-outcome record for a timed-out attempt** → `exactly_one_attempt_outcome_record_for_a_timed_out_attempt`; **timeout retry-eligible by default; a timeout on the final permitted attempt yields terminal `timed-out`** → `timeout_is_retry_eligible_by_default`; **terminal state decided exactly once — `timed-out`, never `abandoned`** → `terminal_state_is_decided_exactly_once`; **a within-timeout attempt is unchanged from T20** → `well_behaved_attempt_within_timeout_is_unaffected`; **the timeout fires regardless of task-worker availability (isolated timer path not regressed)** → `timeout_fires_independent_of_task_worker_availability`. **Retry + jittered-backoff facet added by T22 (032)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/retry_backoff.rs` pins C14's bounded-retry and jittered-exponential-backoff acceptance against the real T22 API (`dagr_core::execution::run_with_retries` for the loop + `RetryConfig` / `Backoff` / `Jitter` / `NoJitter` / `SeededJitter` for the interim knob and injectable jitter, plus the `AttemptEvent::BackoffStarted` phase record), runtime-agnostic (the backoff wait awaits a caller-provided timer future, deterministic seeded jitter — no tokio, no wall-clock sleep), one arch.md acceptance facet per test: **a retry-eligible error is retried up to the configured count and no further** → `retry_eligible_is_retried_up_to_the_budget_and_no_further` (+ `a_single_successful_retry_stops_the_loop`); **a permanent error is not retried regardless of remaining attempts** → `permanent_error_is_never_retried`; **a deliberate skip is not retried** → `deliberate_skip_is_not_retried`; **conservative no-retry default (a single attempt)** → `default_config_performs_exactly_one_attempt`; **backoff is exponential in the attempt index and clamped to the cap** → `backoff_is_exponential_and_capped` (+ `jittered_delay_never_exceeds_the_cap`); **backoff delays are jittered so a fan-out of simultaneous retries does not resynchronize** → `jittered_backoff_does_not_resynchronize_a_fan_out` (+ `seeded_jitter_is_reproducible_and_seed_dependent` for the determinism that makes the schedule assertable); **every attempt produces exactly one attempt-outcome record with gapless increasing attempt numbers, and the terminal node record appears once** → `exactly_one_attempt_outcome_record_per_attempt`; **the backoff interval is a named, monotonic-offset-derived phase distinct from executing** → `backoff_phase_is_a_named_measurable_interval`; **the current attempt number and maximum are exposed to the task (C8)** → `attempt_number_is_visible_to_the_task`; **the next attempt is not started until the prior attempt's closure returned (C1 exclusivity)** → `no_premature_re_entry_attempts_are_strictly_sequential`. **Panic-containment facet added by T23 (033)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/panic_containment.rs` pins C14's **Panics**-paragraph acceptance against the real T23 API (`dagr_core::execution::run_attempt_caught` for the caught single attempt + `run_with_retries_caught` for panic-composed-with-retry, `install_panic_hook`, `check_panic_strategy` / `detect_panic_strategy` / `PanicStrategy`, plus the `AttemptOutcome::Panicked` classification and the `AttemptEvent::AttemptPanicked` outcome record), runtime-agnostic (a dependency-free, `unsafe`-free `catch_unwind` + `AssertUnwindSafe` adapter over the future's poll — no `futures` crate), one arch.md acceptance facet per test: **a panicking task is caught (not unwound) and fails only its own node, mapping to `failed`** → `panicking_task_is_contained_and_fails_only_its_node` (+ `panic_message_is_captured` for the captured detail); **the rest of the run proceeds after a panic** → `independent_node_completes_after_a_panic`; **a panic is a permanent failure, never retried regardless of remaining budget** → `a_panic_is_permanent_and_is_never_retried`; **the panic is attributed to the correct node (task-local state), stable under concurrency** → `panic_is_attributed_to_the_correct_node`; **exactly one attempt-outcome record for a panicking attempt** → `exactly_one_attempt_outcome_record_for_a_panic`; **the binary refuses `panic = "abort"` at startup naming the fix, and permits the unwinding profile** → `startup_check_refuses_abort_and_permits_unwind` (+ `detected_strategy_of_this_binary_passes`); **the hook installs once idempotently and safely under concurrent first-use** → `hook_installation_is_idempotent_and_thread_safe`; **the framework hook chains to / coexists with a pre-existing (test-harness) hook** → `framework_hook_chains_to_a_pre_existing_hook`; **resource poisoning keeps a mid-operation resource out of rotation (author pattern, worked example)** → `resource_poisoning_keeps_a_broken_resource_out_of_rotation`. The `AssertUnwindSafe`-is-load-bearing compile fixture (positive compiles, negative `compile_fail`) ships as doctests on `run_attempt_caught`. **Still deferred (the rest of C14 is owned by later tickets):** the all-workers-blocked timeout-still-fires + SIGTERM-complete-stream *dispatch/isolation* assertion (T33) and the full permit-release outcome matrix under real pools (T37). The single `Test` cell maps the single-attempt facet T20 owns while the note records the T21-landed timeout facet, the T22-landed retry/backoff facet, the T23-landed panic-containment facet, and the deferred T33/T37 facets. No longer `unmapped`. **Run-loop-driven facet added by T24 (034)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/cli/tests/run_loop_driver.rs` drives the *real* C14 attempt path (`run_attempt` / `run_attempt_caught` / `run_attempt_with_timeout`) end-to-end through the M1 run-loop driver, proving the runner runs under a real two-runtime dispatch (attempts on a tasks runtime, the loop/timers/writer on an isolated framework runtime) rather than on a bare test executor: a successful attempt fills the slot and drives its dependent (`happy_path_single_node_terminates`, `linear_chain_drives_dependents`), a deliberate skip terminates the node and the run succeeds (`skip_only_run_reports_success`), and — the framework-isolation half — a per-attempt blocking timeout still fires while a task jams its worker, the stream is still written, and the run still reaches `run-finished` (`framework_survives_a_misbehaving_task`), with the timed-out closure treated as a decided zombie that does not hold the run open and stays `timed-out` (never a second terminal state — `zombie_at_natural_run_end`). The all-workers-blocked *dispatch/starvation* authority stays deferred to T33. **Permit-release outcome-matrix facet added by T37 (047)** — additive, not re-mapping the `Test` cell (which stays T20's headline test): `crates/core/tests/permit_release_matrix.rs` composes C14's timeout/abandonment surface (`TimeoutDecision::mark_blocking_timed_out` + `LateResultBarrier` + `retry_may_start`/`ZombieObserver`, the `AttemptOutcome`/`AttemptEvent` taxonomy) with the C12 ledger to assert the per-class abandonment release rule across the full outcome grid — the C14 half of C12's *"released … for timeout and abandonment only when the underlying work has actually returned, each verified by a test that induces that specific outcome"*. The C14-owned facets it exercises: **a blocking/compute timeout is decided `timed-out` immediately, its permit HELD until the closure returns, and it never becomes a second terminal (`abandoned` arises only on the cancellation path)** → `blocking_and_compute_timeout_hold_the_permit_until_return_and_stay_timed_out`; **a blocking-timeout retry is deferred past the previous closure's return (C1 exclusivity)** → `blocking_timeout_retry_is_deferred_until_the_closure_returns`; **an abandoned attempt's late value is refused by the barrier** (asserted inside the timeout test's `barrier().fill_slot` check); **exactly one attempt-outcome record per attempt across the matrix incl. the timed-out case** → `every_outcome_emits_exactly_one_attempt_outcome_record`. See the C12 row for the full facet list; C14 is no longer deferring T37. |
| C15 | machine | — | stop_mode_cancels_pending_unrelated_default_and_runs_contingency | T34 | **Mapped by T34 (044)** — failure policy, propagation, and the runtime firing of the non-default trigger rules ship in `crates/cli/tests/failure_policy_and_propagation.rs`, driving the *real* T24/T34 run loop (`dagr_cli::driver::drive`) end-to-end and asserting the parsed event stream plus the returned per-node terminal states. The mapped headline test (`stop_mode_cancels_pending_unrelated_default_and_runs_contingency`) covers C15's stop-on-first-failure acceptance — *"no default-rule non-teardown node is admitted after the first terminal failure is observed; a consume-nothing contingency node whose non-default rule fires on the final picture still executes"* and *"pending default-rule nodes unrelated to the failure end `cancelled`"*: with the memory pool pinned so admission is serialized, `bad` fails before the unrelated default-rule `later` is admitted, so `later` ends `cancelled` (no `attempt-started`) while the zero-cost `any-failed` `contingency` still runs. Siblings in the same suite cover the rest of C15's owned surface, one acceptance criterion each: **`all-terminal` cleanup fires after an upstream failure, verified under both modes** → `all_terminal_cleanup_fires_after_a_failure_in_both_modes`; **`any-failed` fires on a failure-like upstream** → `any_failed_contingency_fires_on_a_failure`; **`any-failed` contingency that never arose is `skipped` and the run succeeds** → `any_failed_contingency_never_arose_is_skipped_and_run_succeeds`; **no node runs on a non-succeeded data dependency; propagated-state selection by class** → `failed_data_upstream_propagates_upstream_failed`, `skipped_data_upstream_propagates_upstream_skipped_and_run_succeeds` (the latter also **skip-only run reports success**); **continue-independent completes an unrelated branch** → `continue_independent_runs_unrelated_branch`; and **every node — including never-ran nodes — has exactly one terminal state** → `every_node_has_exactly_one_terminal_state`. The teardown-ordering carve-out under stop mode (C17) is a documented, deliberately-unimplemented seam left to T52; cancellation *mechanics* (the run-scoped signal, grace, `abandoned`) are T35 — this ticket only *decides* which nodes become `cancelled`. |
| C16 | machine | platform-conditional | cancelled_abandoned_and_failed_are_distinct | T35 | **Mapped by T35 (045)** — the cancellation **core + graceful drain** (the mechanics C15/T34 only *decided* against) ship in `crates/cli/tests/cancellation_core_and_drain.rs`, driving the **real** T24/T34 run loop (`dagr_cli::driver::drive`) end-to-end and asserting the parsed event stream plus the returned per-node terminal states and recorded [`CancellationOrigin`](dagr_core::context::CancellationOrigin). Cancellation is triggered by a **programmatic** in-process handle (`RunConfig::cancel_handle` → `driver::CancelHandle`, the internal entry point T36's OS-signal handler will fire) — **no OS signals here** — and cooperation is controlled by a shared flag + observable barriers, so CI is deterministic (no wall-clock/network). The mapped headline test (`cancelled_abandoned_and_failed_are_distinct`) covers C16's load-bearing acceptance — *"a task that observes cancellation and returns promptly is recorded `cancelled`; one that does not return within grace is recorded `abandoned` — both distinct from `failed`"* — with three concurrent nodes landing `cancelled`, `abandoned`, and `failed` respectively and no cross-contamination. Siblings in the same suite cover the rest of the T35-owned C16 surface, one acceptance facet each: **the run-scoped token cancels all live per-attempt children exactly once and is idempotent** → `run_token_cancels_all_children_once_and_is_idempotent`; **a child cancel touches neither its sibling nor the parent run token** → `child_cancel_does_not_touch_siblings_or_parent`; **a prompt cooperative observer is recorded `cancelled`, filling no slot, with a complete parseable stream** → `prompt_cooperative_observer_is_cancelled`; **non-returning work is `abandoned` after grace and the run still terminates (no hang), never a second terminal state** → `non_returning_work_is_abandoned_after_grace_and_run_terminates`; **no new admission after cancellation — a ready-but-unstarted node is settled `cancelled`, not executed** → `no_new_admission_after_cancellation`; **stop-on-first-failure routes through the cancellation core recording a failure origin so C26 can prefer run failure** → `stop_on_first_failure_routes_through_cancellation_core_with_failure_origin`; **the grace period defaults to 10 s and an operator override is honoured** → `grace_default_is_ten_seconds_and_override_is_honoured`; **the worst-case shutdown budget (grace + teardown deadline 15 s + final flush 2 s = 27 s at defaults) is computed and printed and reflects the flags** → `shutdown_budget_is_grace_plus_teardown_plus_flush_and_reflects_flags`; **a non-cancelled run is unchanged (no origin, T24/T34 behaviour intact)** → `non_cancelled_run_is_unchanged`. The core token primitives live where the type does (`dagr_core::context`: `CancellationSource::child`/`cancel`/`is_cancelled`, the per-attempt-child signal threaded into each `RunContext`, and `CancellationOrigin`). **Signal handling stays platform-conditional** (SIGTERM/SIGINT; Linux fully, macOS documented divergences) and is **deferred to T36** (gated by T70): the OS-signal→cancel wiring, the final event-stream flush with fsync, the bounded-wait/distinct-exit-code for an unwritable sink at shutdown, and the per-run temp-dir cleanup. **Also deferred:** teardown under a fresh uncancelled signal + the teardown deadline itself (C17/T52 — this ticket only consumes the teardown-deadline value for the budget), the permit-release outcome matrix and zombie permit accounting under cancellation (C12/T37 — this ticket produces the `cancelled`/`abandoned` classifications those tests assert against), and the final C26 exit-code selection (this ticket records the origin, it does not own the code table). No longer `unmapped`. **OS-signal / final-flush / temp-cleanup facet added by T36 (046)** — additive, not re-mapping the `Test` cell (which stays T35's headline test): `crates/cli/tests/os_signals_flush_and_cleanup.rs` lands the OS-signal half of C16 T35 deferred, driving the **real** T24/T35 run loop (`dagr_cli::driver::drive`) and the new `dagr_cli::signals` / `dagr_cli::temp` modules, asserting the parsed stream, the returned per-node terminal states, the `ShutdownExit` selection, and the on-disk temp dirs. Determinism (CI): OS signals are **never** raised at the test-runner *unprotected* — the signal→cancel *wiring* is proven through the same `CancelHandle` seam the installed handler drives (`a_signal_routes_to_the_cancel_handle_seam`, `SignalRouter`) plus a re-entry-hardening routing unit test (`a_second_signal_is_idempotent_and_does_not_shortcut_the_flush`, `route_signal`), and the **end-to-end** path raises a *real* `SIGTERM`/`SIGINT` **after** installing the handlers (so the signal is *caught*, never fatal to the harness) and asserts the run cancels with an external-interrupt origin and a complete stream (`real_sigterm_triggers_cancellation_and_a_complete_stream`, `real_sigint_triggers_cancellation_and_a_complete_stream`, serialized + `#[cfg(unix)]`; non-unix is a documented no-op — platform-conditional under T70). One acceptance facet per test: **SIGTERM/SIGINT each fire the same `CancelHandle` seam and are interchangeable** (the two real-signal tests); **a second signal is re-entry hardened — it does not shortcut the final flush** (`a_second_signal_is_idempotent…`); **a complete, fsync'd stream is written on the cancellation path with exactly one run-end fsync** → `complete_fsyncd_stream_on_cancellation`; **and on the normal path** → `complete_fsyncd_stream_on_normal_end`; **an unwritable sink at shutdown yields a bounded wait + the distinct sink-failure exit, never a hang/success/plain-cancellation** → `unwritable_sink_at_shutdown_yields_bounded_wait_and_sink_failure_code`; **the sink fault is not a run failure** → `sink_failure_at_shutdown_is_not_a_run_failure`; **run failure wins the C26 precedence over sink failure** → `run_failure_wins_over_sink_failure_precedence`; **two runs get disjoint per-run temp dirs (confinement)** → `two_runs_get_disjoint_temp_directories`; **the next invocation reclaims a leftover temp dir regardless of how the prior process ended, keeping reserved outputs** → `next_invocation_reclaims_leftover_temp_dir_but_keeps_outputs`; **cleanup removes the temp dir** → `cleanup_removes_temp_dir`; **cleanup is best-effort on a missing dir (abandon path)** → `cleanup_is_best_effort_on_missing_dir`; **a cooperative task cleans its own temp artifact on cancellation and the driver reclaims the whole per-run dir** → `cooperative_task_cleans_temp_on_cancellation_and_driver_reclaims_dir`. The per-run temp dir is reachable through the context (`dagr_core::context::RunContext::temp_dir`, threaded by the driver). **Still deferred:** the full C26 exit-code *table* and numeric values (T55 — this ticket reports the `ShutdownExit` selection by cause, not numbers), teardown under a fresh uncancelled signal + the teardown deadline (C17/T52), and the platform matrix that runs the unix signal facet on both tiers (T70). |
| C17 | machine | — | unmapped | T52 | Teardown runs after any terminal state; failing teardown recorded but doesn't change outcome; runs under a fresh signal even when cancelled; resume re-executes teardown-covered nodes (T52). |
| C18 | machine | — | unmapped | T53 | Value written attempt-one readable attempt-two; keys namespaced by run+node; cross-node read enforced; durable-store resume carry-forward; I/O failure retry-eligible (T53/T54a/T54b). |
| C19 | machine | platform-conditional | envelope_completeness | T19 | **Mapped by T19** (`crates/artifact/tests/event_stream_writer.rs`, test `envelope_completeness`): every record carries run id + schema version + gapless sequence + wall stamp + monotonic offset, over the append-only JSONL writer. The rest of the C19 suite in the same file asserts the sibling facets — `gapless_strictly_increasing_sequence`, `offsets_are_monotonic_and_authoritative`, `run_started_carries_full_header`, `write_through_not_buffered`, `fsync_once_at_run_end`/`fsync_once_at_cancellation`, `abrupt_kill_parseability` (≤1 trailing partial), `concurrent_run_disjointness` (disjoint simultaneous streams), `mid_run_sink_failure_surfaces_fault`, `foldability_with_no_original_run_access`, `canonical_bytes_are_deterministic_and_sorted` (T4 §6). **Flush/fsync behavior stays platform-conditional** (different fsync semantics on macOS; the ≤1-trailing-partial tolerance is the platform-sensitive facet), and the abrupt-process-kill/fault-injection suite (T27) and the end-to-end two-runs test (T67) still layer on top, flush facet gated by T70. No longer `unmapped`. **Run-loop-driven facet added by T24 (034)** — additive, not re-mapping the `Test` cell (which stays T19's headline test): `crates/cli/tests/run_loop_driver.rs` drives the *real* C19 writer through the M1 run-loop driver, proving the driver-owned records the writer defines but does not emit itself — `run-started` carrying the full header (identity, pipeline, both fingerprints, parameters, data interval, allowlisted env) written **before** assembly is acted on so an assembly failure still records itself (`run_started_header_carries_start_fields`, `assembly_failure_still_records`), the `node-ready` / `node-admitted` / attempt-transition / `node-terminal` sequence in order (`happy_path_single_node_terminates`), the `zombie-at-exit` event before `run-finished` (`zombie_at_natural_run_end`), a UUIDv7 identity minted at bootstrap and operator-overridable on every record (`identity_is_a_uuidv7_minted_at_bootstrap`, `operator_override_replaces_the_minted_identity`), the allowlisted-env-only capture with a non-allowlisted-secret negative check (`allowlisted_env_captured_others_not`), and two concurrent runs writing disjoint, independently-valid streams each carrying its own identity (`two_simultaneous_runs_do_not_interfere` — the driver-side counterpart of T19's writer-level `concurrent_run_disjointness`, ahead of the dedicated T67 two-runs test). **Crash-safety / I/O fault-injection facet added by T27 (037)** — additive, not re-mapping the `Test` cell (which stays T19's headline test): `crates/artifact/tests/crash_safety_fault_injection.rs` ships the C19 crash-tolerance and sink-failure acceptance criteria the earlier suites only smoke-tested, driving the **real** merged T19 writer (`dagr_artifact::event_stream::{EventStreamWriter, read_records, SinkFault}`) through an **injected controllable fault sink** (the T0.6 §1 seam) and asserting against the real tolerant reader. A "crash" is simulated **deterministically** — the genuine writer's on-disk bytes truncated at **seeded** offsets *at / before / part-way through* each event write (no real process kill, no wall-clock/network; every randomized trial records its seed for replay) — so the acceptance holds without CI-flaking. One arch.md C19 facet per test: **abrupt kill → valid prefix** across seeded trials (`abrupt_kill_at_a_random_point_yields_a_valid_prefix`); **≤1 trailing partial, never two** around every write, with the reader's tolerance guarded against being too lax (`kill_mid_write_leaves_at_most_one_partial_never_two`); **gapless-and-strictly-increasing sequences after a kill** (`sequence_numbers_are_gapless_after_a_kill`); **a one-record prefix still identifies its run** via the full run-started header (`a_one_record_stream_still_identifies_its_run`); **every record carries run identity + schema version** (`every_record_carries_run_identity_and_schema_version`); **interior corruption is rejected** (`interior_corruption_is_rejected`); the **induced mid-run failing sink surfaces the documented `event stream unwritable` fault by cause, leaving no sequence gap and a valid gapless prefix** (`induced_mid_run_failing_sink_surfaces_the_sink_failure_cause`); **disk-full mid-run behaves as a sink failure** (`disk_full_mid_run_behaves_as_a_sink_failure`); **disk-full at final flush reports the sink-failure cause, not a corrupt success** (`disk_full_at_final_flush_produces_the_sink_failure_cause`); **slow/unwritable sink at shutdown is a bounded wait within a fixed timeout, never a hang** (`slow_or_unwritable_sink_at_shutdown_is_a_bounded_wait_not_a_hang`); **the sink-failure code is asserted by its documented cause, never a magic number** (`sink_failure_is_asserted_by_cause_not_by_literal`); **a genuine run failure is not masked by the self-inflicted `cancelled` a sink fault drives** (`run_failure_is_not_masked_by_self_inflicted_cancellation`); **two concurrent runs write disjoint, individually-valid streams that partition cleanly by run identity** (`two_concurrent_runs_produce_disjoint_individually_valid_streams`); and **the seeded kill points are reproducible** so a failure replays (`seeded_kill_points_are_reproducible`). Because the merged T24 driver deliberately absorbs a sink fault (T24 scoped the cancel-and-exit *reaction* out to T36/T55), the "moves to cancelling / exits with the sink-failure code" contract is asserted at the writer/sink seam where the `SinkFault { reason: "event stream unwritable" }` actually surfaces (T0.6 §5/§6 — the seam this ticket binds against) resolving the code by cause, never by literal; the OS-signal wiring (T36), the fold of a crashed stream into an artifact (T42/T68 — SL3), and the full two-runs test (T67) still layer on top. **Two-concurrent-runs facet added by T67 (048)** — additive, not re-mapping the `Test` cell (which stays T19's headline test): `crates/cli/tests/two_concurrent_runs.rs` drives **two** real M1 runs (`dagr_cli::driver::drive`, T24) **concurrently in one process**, each on its own OS thread with its **own** injected sink + clock and its **own** `RunConfig` (own base / own run id), forcing a genuinely overlapping write window via a shared width-2 `Barrier` the two `gate` source tasks rendezvous at — deterministic (synchronised on the observable barrier signal, never a sleep, never an ordering assumption between the two runs). One arch.md C19 facet per test: **two simultaneous runs mint distinct identities, each stream is independently valid (parses, opens run-started / closes run-finished, one real schema version, gapless strictly-increasing seq from 0) and carries ONLY its own identity with no foreign record, and both reach their own correct terminal outcome** → `two_concurrent_auto_minted_runs_stay_disjoint`; **the two streams concatenated (in the opposite of launch order) and partitioned by run identity reproduce each run's stream record-for-record, order-independently because identity travels on every record** → `concurrent_streams_concatenate_and_partition_losslessly`; **two runs against a SHARED base with distinct operator-overridden ids write disjoint on-disk `<base>/<pipeline>/<run-id>/` directories (exactly the two per-run dirs exist, disjoint subtrees, no shared/overwritten/collided file, disjoint reported stream paths) and both streams still partition cleanly — the store-layout guarantee, proven not to be a UUIDv7-monotonicity artefact** → `concurrent_runs_write_disjoint_directories_under_a_shared_base`. Non-vacuous (a shared id or a shared sink would fail the distinct-identity, no-foreign-record, and directory-count assertions) and hermetic (per-process temp base, no external services, no wall-clock ordering). The tool relies on **no** inter-process coordination between the two runs (arch.md `## Operational model` — "the tool does not coordinate between processes"): the guarantee holds because the store partitions by identity, not because the two runs cooperate. Consumes T19/T24/T0.6 only; changes no production behaviour. This is the *end-to-end two-runs test* the T19/T24/T27 notes above pointed to. |
| C20 | machine | — | every_node_appears_once_with_its_complete_fields | T40 | **Mapped by T40 (051)** — the C20 graph-artifact emitter (`dagr_cli::graph`) serializes a real assembled `dagr_core::Pipeline` to a schema-valid canonical-JSON artifact, offline and deterministically, one arch.md C20 acceptance criterion per test in `crates/cli/tests/graph_artifact_emission.rs` against the real emitter. The mapped headline test (`every_node_appears_once_with_its_complete_fields`) covers arch.md C20's *"every node and edge that the running pipeline would use appears in the artifact, including declared resource requirements"* — each assembled node appears exactly once carrying name, group, stable task/input/output type names, execution class, and dependency lists; none missing, none invented. Its siblings cover the rest of C20's owned surface: **produced in an empty environment** → `emission_succeeds_in_an_empty_environment`; **byte-identical outside generation time** → `emitting_twice_is_byte_identical_outside_generation_time`; **generation time is the only variance** → `generation_time_is_the_only_field_that_varies`; **full effective policy incl. defaults, all-default == every-default-written-out** → `full_effective_policy_is_written_out_with_defaults` (+ `no_policy_and_written_out_defaults_emit_identical_policy_blocks`); **declared resource requirements in native units** → `declared_resource_requirements_are_present_in_native_units`; **edge kinds + carried type names** → `data_edges_are_tagged_and_carry_the_stable_type_name`; **stable declared names, never `type_name` as identity** → `recorded_names_are_stable_declared_names_never_type_name`; **build-provenance header embedded at build time** (the Stability/supply-chain obligation folded into this row, below) → `header_carries_the_versioned_provenance_slots`; **two in-process assemblies agree (interlock with T15)** → `two_in_process_assemblies_emit_identically`; **graph verb reachable, no store/parameters (C26/C7)** → `graph_verb_writes_to_a_sink_with_no_store_or_parameters`; **a stable-name-less / malformed-stable-name node is not emittable** → `a_node_without_stable_names_fails_emission` / `a_malformed_stable_name_fails_emission`; and a real emitted artifact is checked in as the T40 corpus fixture (`tests/fixtures/corpus/graph/v1/t40-three-node.json`, validated by the T39 corpus walker) kept equal to a live emission by `checked_in_corpus_fixture_matches_a_real_emission`. **The published-schema round-trip** — arch.md C20's *"the artifact validates against its published schema"* — is `emitted_artifact_validates_against_the_published_schema` in `crates/cli/tests/graph_artifact_schema_roundtrip.rs`: a real emitted artifact validates against `schemas/graph/v1.schema.json` via the T39 helper (`dagr_artifact::schema::validate_value`), and a corrupted copy (a required field removed) is rejected, proving the check has teeth. That suite is behind the CI-/dev-scoped `schema-validation` feature (its `jsonschema` validator is not a runtime dependency, T4 ADR 017 §4), so — exactly like T39's own `artifact_schemas` suite — it is not the `cargo test --workspace -- --list` id the verifier binds to; CI runs it in the dedicated `graph-artifact schema round-trip (T40)` step. The **author-declared stable-name trait** (`dagr_core::stable_name::StableName`, the T0.7 §1 contract) lands here as C20's first consumer, captured at registration through the stable-name-aware registrars (`Flow::register_source_named` / `register_named`) into `PipelineNode::stable_names`, covered by `crates/core/tests/stable_names.rs`. **Deferred (Out of scope):** the structural fingerprint / policy hash *values* and algorithm version are T41 (C21) — this ticket carries the reserved header slots only; the diagram renderer is T46 (C24); ordering-edge authoring is T50 (C4); artifact-validation/compat CI beyond this artifact's own schema is T48; and the run artifact / event-stream fold is T42 (C22). No longer `unmapped`. |
| C21 | human | — | release-checklist | — | "documentation-at-point-of-use (C21)" is a judgment (criterion 8). C21's mechanical sub-criteria are machine-tested under T41; the criterion's governing class is human. |
| C22 | machine | — | never_ran_nodes_covered | T42 | **Mapped by T42** (`crates/artifact/tests/run_artifact_fold.rs`): the standalone C22 fold (`dagr_artifact::fold::fold_stream`) is exercised by the covering suite. `never_ran_nodes_covered` is the headline node-coverage acceptance — every graph node appears at least once, never-ran nodes carrying `upstream-failed` / `upstream-skipped` (with originating identity) / originated `skipped`. Sibling tests in the same file cover the rest of C22's owned surface: `phases_sum_exactly` (phase durations sum bit-exactly to the attempt total, both from monotonic offsets), `one_record_per_attempt_not_per_node`, `interrupted_marking_on_truncation` + `trailing_partial_tolerance_boundary` (crashed-run fold marked interrupted, one-trailing-partial C19 tolerance), `assembly_failed_variant` / `bootstrap_failed_variant` (zero attempts, full error list, fingerprint present only when assembly succeeded), `allowlist_negative_planted_sentinel` (no env value outside the declared allowlist survives the fold), the summary trio (`summary_retained_values` / `summary_peak_slot_residency` / `summary_zombie_pinned_time_and_capacity`), and `determinism`. The structural-fingerprint-joins-a-same-build graph artifact facet is `run_artifact_structural_fingerprint_matches_graph_artifact` (`crates/cli/tests/run_artifact_fold_fingerprint_join.rs`); the REAL-folded-artifact schema round-trip is `folded_full_run_validates_against_published_schema` (`crates/artifact/tests/run_artifact_fold_schema.rs`, behind the `schema-validation` feature). **End-to-end real-writer → fold interlock added (T42 continuation)** — `e2e_real_writer_stream_folds_and_validates_against_run_schema` and `e2e_real_writer_truncated_stream_folds_interrupted_and_validates` (same file, behind `schema-validation`) drive an **actual `EventStreamWriter`** (the reconciled C19 writer) through a multi-node run with a retry and a never-ran node, fold the REAL emitted bytes, and assert the folded artifact validates against `schemas/run/v1.schema.json` (one attempt record per attempt incl. the retry ⇒ 2, phases summing to each attempt total from real offsets, never-ran coverage, complete header) — the guarantee the hand-built suite could not give (non-vacuous: mis-keying the writer's `attempt-outcome` kind drops the fold's attempt count and fails validation). The truncated variant drops the trailing `run-finished`, byte-cuts the final record, and asserts the interrupted representation — `overall_outcome` stays `cancelled` inside the closed enum while the crash-truncation signal is a **first-class top-level `interrupted` artifact field** (open-world schema, additive; T0.10) — still schema-valid against the unmodified schema. Prior-version-fixture parseability in CI forever remains T48. **Critical-path enrichment completed by T43 (054)** — additive, not re-mapping the `Test` cell (which stays T42's `never_ran_nodes_covered` headline): T42 stood up the summary skeleton and left `critical_path_ns` a conservative longest-attempt placeholder; T43 makes the two headline numbers correct — **total elapsed** from monotonic offsets only and **critical-path time** as the longest dependency-respecting chain of node `executing` contributions (fixed by `docs/adr/0001-critical-path-definition.md`: retries collapse by summing executing; ready-wait/permit-wait/backoff/zombie excluded; predecessors reconstructed from `node-ready`/terminal offsets, so the number is a pure function of the artifact with no edge list). Covered by `crates/artifact/tests/run_summary_critical_path.rs` (11 tests) and the real-artifact-with-critical-path schema round-trip `folded_run_with_critical_path_validates_against_published_schema` (`crates/artifact/tests/run_artifact_fold_schema.rs`, `schema-validation` feature — schema UNCHANGED). See the SL5 row for the full T43 contribution note. No longer `unmapped`. |
| C23 | machine | — | a_task_attaches_a_novel_metric_with_no_framework_change | T44 | **Mapped by T44 (055)** — the C23 node-metrics facility lands in `dagr_core::metrics` (`AttemptMetrics`, `MetricValue`, `MetricError`, the `AttributingAllocator` + `AttemptScope`, and the `dagr.`-prefixed built-in metric names), one arch.md/T44 acceptance facet per test in `crates/core/tests/node_metrics.rs`. The mapped headline test (`a_task_attaches_a_novel_metric_with_no_framework_change`) covers C23's *"A task can attach a new measurement with no framework change"* — an open, unschematized set where a novel name is accepted with no enum/registry edit. Its siblings cover the rest of C23's owned surface, one criterion each: **numeric-only values** (the type surface refuses a non-numeric value) → `numeric_value_shapes_are_all_accepted_and_stored_as_numbers`; **reserved `dagr.` prefix rejected loudly at attach time, naming the metric, value not recorded** → `reserved_prefix_is_rejected_at_attach_time_naming_the_metric` (+ the boundary case `a_name_that_merely_contains_but_does_not_start_with_the_prefix_is_accepted`); **128-entry cap with deterministic order-independent survivors and recorded truncation** → `entry_count_cap_truncates_deterministically_and_records_it`; **16 KiB byte cap with deterministic recorded truncation** → `byte_size_cap_truncates_deterministically_and_records_it`; **truncation record does not re-trigger a cap violation** → `recording_truncation_does_not_re_trigger_a_cap_violation`; **framework metrics (peak memory, permit sizes, phase timings) present with no task metrics, all under `dagr.`** → `framework_metrics_present_when_the_task_attaches_nothing`; **units-in-name convention holds for every built-in** → `every_builtin_metric_name_follows_the_units_in_name_convention` (cited by `docs/conventions/metric-naming.md`); **peak memory attributed per-attempt via task-local state, non-cross-contaminating under concurrency** → `peak_memory_is_per_attempt_not_process_wide_under_concurrency`; **peak tracks the high-water mark, not the residual** → `peak_memory_tracks_the_high_water_mark_not_the_residual`; **allocations outside any attempt are unattributed and the allocator behaves correctly** → `allocations_outside_any_attempt_are_unattributed_and_the_allocator_is_correct` (the `AttributingAllocator` installs as this test binary's `#[global_allocator]`). **Metrics reach the run artifact unmodified via T42's fold** → `crates/artifact/tests/node_metrics_fold.rs::collected_metrics_reach_the_artifact_byte_for_value_identical`, and the REAL metrics-carrying folded-artifact schema round-trip (schema UNCHANGED — the `metrics` object is the open numeric map `schemas/run/v1.schema.json` already declares) → `a_real_metrics_carrying_folded_artifact_validates_against_the_published_run_schema` (same file, behind the `schema-validation` feature). **Deferred (Out of scope):** rendering/annotating metrics in diagrams (C24/T46/T47) and run-summary aggregation like critical path / peak slot residency (C22/T43) — this ticket produces only the per-attempt metric set. No longer `unmapped`. |
| C24 | human | — | release-checklist | — | "diagram readability (C24)" is a judgment (criterion 8). C24's mechanical proxies (DOT/Mermaid parse in CI; golden tests) are machine-tested under T46/T47; the criterion's governing class is human. **T46 (057)** shipped the base renderer (`dagr_render`, reading a graph artifact only — no edge onto `dagr-core`, so "rendering requires no access to the producing binary" is a crate-graph fact) with those mechanical proxies in `crates/render/tests/`: every node/edge appears in DOT and Mermaid, data vs ordering edges carry disjoint documented styling and data edges are labelled with the carried type (`renderer.rs`), byte-stable golden `.dot`/`.mmd` files (`golden_dot_is_byte_stable` / `golden_mermaid_is_byte_stable`), a schema-invalid artifact is rejected with a named diagnostic (`rejects_a_schema_invalid_artifact`), and the two output formats are accepted by their reference tools with teeth — `dot` parses and the browserless Mermaid parser (`mermaid.parse` under Node, no mmdc/Chromium) accepts, malformed input rejected (`reference_tools.rs`, run in CI's `render-reference-tools` job under `DAGR_REQUIRE_RENDER_TOOLS=1`). **T47 (058) added the run-overlay proxies** (additive; the `Test` cell stays the human release-checklist since C24's governing class remains human): `dagr_render::overlay` projects a C22 run artifact onto the base diagram in both formats — a documented, distinct per-state style for every one of the normative nine terminal states plus the `not-requested` single-node-replay marking (published as a rustdoc mapping table on the overlay entry point), with originated skips/failures (`skipped`, `failed`, `timed-out`) visually separable from propagated ones (`upstream-skipped`, `upstream-failed`), and a per-node duration annotation. Covered in `crates/render/tests/overlay.rs` (16 tests): `overlay_is_opt_in_no_regression` (no run artifact ⇒ byte-for-byte the T46 golden), `every_state_distinct_documented_style_dot`/`_mermaid`, `originated_vs_propagated_skip_distinguishable`, `originated_vs_propagated_failure_distinguishable`, `cancellation_family_distinct`, `satisfied_from_prior_distinct_from_succeeded`, `single_node_replay_not_requested_handled`, `duration_annotations_present_and_correct`, `every_node_and_edge_present_with_overlay`, `overlaid_30node_golden_is_byte_stable` (byte-stable overlaid `.dot`/`.mmd` goldens), `works_on_a_historical_artifact` + `real_folded_run_artifact_overlays` (a REAL T42-folded run artifact and the frozen run-artifact corpus, no producing binary — a crate-graph fact), and `graph_run_mismatch_is_defined_not_fatal` (a graph-only node renders un-styled; an extra run record is reported in a diagram comment, never a phantom node — neither panics). The overlaid DOT is accepted by `dot` and the overlaid Mermaid by the browserless Mermaid parser via `dot_reference_tool_accepts_the_overlaid_dot` / `mermaid_reference_tool_accepts_the_overlaid_mermaid` (`reference_tools.rs`). The criterion's governing class remains human. |
| C25 | machine | — | a_task_line_and_a_third_party_line_both_carry_node_and_attempt | T45 | **Mapped by T45 (056)** — the C25 logging/tracing integration lands in `dagr_cli::logging` (the single process-global subscriber, `OutputMode`, `attempt_span`/`attempt_span_from`), keeping `dagr-core` dependency-free (the `tracing` dep is in `dagr-cli`, not core; core exposes only the dep-free `LogSpan` identity payload on the C8 `RunContext`). The driver instruments each attempt future with the span (`.instrument`). One arch.md/T45 acceptance facet per test in `crates/cli/tests/logging_and_tracing.rs`. The mapped headline test (`a_task_line_and_a_third_party_line_both_carry_node_and_attempt`) covers C25's *"Any log line produced during an attempt can be traced to its node and attempt without timestamp correlation"* — a task-emitted line and a third-party library line both carry the node + attempt as discrete span fields. Siblings cover the rest of C25's owned surface, one criterion each: **third-party lines inherit the attempt span** → `a_third_party_line_inherits_the_attempt_span`; **lines from concurrently executing nodes are unambiguously separable by span fields** → `concurrent_node_lines_are_separable_by_span_fields`; **retry attempts distinguishable (same node, different attempt number)** → `retry_attempts_share_the_node_but_differ_by_attempt_number`; **structured is the default and machine-parseable (run/node/attempt as discrete queryable fields)** → `structured_is_the_default_when_no_mode_env_is_set` + `structured_mode_emits_machine_parseable_records`; **human-readable mode via the env var with no code change; unset/unrecognized falls back deterministically to the documented default** → `the_environment_variable_selects_human_readable_output` + `human_mode_is_not_json_but_still_carries_the_fields` + `an_unrecognized_mode_falls_back_to_the_documented_default` (+ `the_documented_env_var_name_is_stable` pinning the `DAGR_LOG_FORMAT` name); **marked secret never appears on framework output paths (planted sentinel), in both output modes** → `a_planted_secret_never_surfaces_on_framework_output_structured` + `a_planted_secret_never_surfaces_on_framework_output_human`; **a task-authored leak is outside the guarantee (the boundary, documented)** → `a_task_authored_leak_is_outside_the_guarantee` (with the boundary stated in rustdoc on `dagr_cli::logging`); **exactly one process-global subscriber, installed once, coexists with the test harness, no double-install** → `installing_the_global_subscriber_is_idempotent_and_coexists`; **the span follows the attempt future across `.await` points (driver instrumentation seam)** → `an_async_attempt_future_inherits_the_span_across_awaits`. The C25 tracing layer is the developer/operator observability surface and is deliberately **distinct** from the C19 durable event stream (the authoritative record). **Deferred (Out of scope):** the M4 library-owned CLI flag for mode selection (C26 — M3 ships env-var selection only); live-telemetry/push-export sinks (a C19 stream-tailing concern); redacting task-authored content (explicitly outside the guarantee). No longer `unmapped`. |
| C26 | machine | — | unmapped | T55 | Verbs behave identically across pipelines; validate exits non-zero + prints all problems; exit-code table exhaustive; no-args prints verbs cleanly; fold produces the interrupted artifact (T55/T56/T68). |
| C27 | machine | — | unmapped | T57 | Structural mismatch refuses + prints diff; satisfied node not re-executed; undemanded prior success is satisfied-from-prior; dangling durable reference fails; no-op resume of a full success; lineage links parent and root (T57/T58/T59). |
| C28 | machine | — | unmapped | T60 | Sync single-task test needs no runtime; structure test fails on add/remove/rename/rewire/regroup/policy-change and not on rebuild/toolchain-bump; single documented fixture-update flow; fault-injection suite (T60/T61/T62). |

## System-level criteria (criteria 1–8)

| Criterion | Class | Platform | Test | Covered-by | Notes |
|---|---|---|---|---|---|
| SL1 | machine | — | unmapped | T64 | **machine** — README quickstart compiles and runs verbatim in CI, empty directory to an artifact-inspected two-node pipeline (T7/T64). The "under thirty minutes" walkthrough is the **human** facet, audited on the release checklist (recorded under SL8human), not a CI timer. |
| SL2 | machine | — | unmapped | T12 | Mis-wiring two tasks is a compile error whose message contains both type names; UI tests on the pinned toolchain. T8 shipped the pinned-toolchain UI harness (`crates/core/tests/ui.rs`) and the both-type-names assertion; the mis-wiring case against the real authoring API lands in T12, so this stays `unmapped`/deferred to T12. |
| SL3 | machine | — | unmapped | T27 | Every run produces artifacts — normal, crashed (fold verb), cancelled, assembly/bootstrap-failed (T27/T42/T68). |
| SL4a | machine | — | unmapped | T65 | *Structural* determinism — two builds of the same source produce identical fingerprints and byte-identical graph artifacts (generation time aside), on different toolchains (T15/T41/T65). **T41 (052) contribution:** the C21 fingerprint *values* are now computed and their determinism / change-matrix / registration-order / cross-toolchain-input-exclusion facets are covered by real tests — `crates/core/tests/fingerprint.rs` (structural-fp stable-name & carried-type coverage, add/remove/rename/rewire matrix, registration-order independence, repeated-computation determinism, algorithm-version identifier, `Pipeline::fingerprint()` reuse surface) and `crates/cli/tests/fingerprint_artifact.rs` (computed values wired into the graph header, version-prefixed strings, generation-time / provenance excluded from both hashes). The **cross-toolchain byte-identity** half runs in CI as the `cross-toolchain-fingerprint` job (`.github/workflows/ci.yml`), which builds and runs the `crates/cli/examples/fingerprint_fixture.rs` fixture under two toolchains and diffs the two fingerprint strings, failing on any divergence. The byte-identical-graph-artifacts facet is T15's (`crates/core/tests/determinism_and_purity.rs`). The FULL SL4a acceptance (fingerprints + byte-identical artifacts, both on two toolchains, as one gate) lands at **T65**; this row stays `unmapped`/deferred to T65 because SL4a's single-`Test` binding is the T65 acceptance gate, not any one contributing suite. Fingerprint algorithm v1 uses dependency-free FNV-1a (BLAKE3 unavailable under the MIT-only supply-chain policy — see `docs/implementation/DEVIATIONS.md`), which is fully deterministic across toolchains. |
| SL4b | machine | — | unmapped | T62 | *Interpretive* determinism — the same recorded outcomes yield the same terminal states, propagation decisions, and artifact, replayed through the C28 harness (T62/T65). |
| SL4c | disclaimer | — | — | — | What tasks *do* against external systems is theirs; the tool claims nothing about it. Carried here **present and unclassified-as-testable** so it is never silently absent (arch.md criterion 4(c)). |
| SL5 | machine | — | unmapped | T43 | A run's duration and resource profile are answerable entirely from artifacts, with no access to the producing machine (T43/T65). **T43 (054) contribution:** the two headline C22 summary numbers a duration/resource readout needs — **total elapsed** (the monotonic wall, last offset − start, never the informational `wall` stamps) and **critical-path time** (the longest dependency-respecting chain of node `executing` contributions; retries collapse by summing executing, ready-wait/permit-wait/backoff/zombie excluded per `docs/adr/0001-critical-path-definition.md`) — are now computed as a **pure function of the folded artifact** (`dagr_artifact::fold`, reconstructing dependency predecessors from `node-ready`/terminal offsets, no store / no live run / no edge list). Covered by real tests in `crates/artifact/tests/run_summary_critical_path.rs`: `total_elapsed_is_monotonic_not_wall_clock`, `structure_limited_run_reads_as_structure_limited` / `resource_limited_run_reads_as_resource_limited` (the two numbers together discriminate structure- from resource-limited), `critical_path_respects_dependencies_not_raw_duration`, `retries_collapse_per_the_adr`, `permit_wait_treatment_matches_the_adr`, `zombie_pinned_time_is_separated_from_the_path`, `pure_function_of_an_artifact`, and the edge cases `single_node_graph` / `never_ran_node_on_the_chain_contributes_zero` / `empty_zero_attempt_run_is_well_defined`; the REAL-folded-artifact-with-critical-path schema round-trip is `folded_run_with_critical_path_validates_against_published_schema` (`crates/artifact/tests/run_artifact_fold_schema.rs`, behind the `schema-validation` feature — the schema is UNCHANGED, `critical_path_ns` is an existing field). The FULL SL5 acceptance (the whole run's profile answerable from artifacts on a machine with no access to the producer) lands at **T65**; this row stays `unmapped`/deferred to T65 because SL5's single-`Test` binding is the T65 acceptance gate, not any one contributing suite. |
| SL6 | machine | — | unmapped | T61 | Adding a node requires no changes outside its own module, the assembly site, the structure fixture, and (new resource) registry construction in `main` — structure-diff on a reference pipeline (T61/T65). |
| SL7 | machine | — | unmapped | T65 | Nothing requires a server, database, or scheduler; the binary and its arguments suffice to run and produce local artifacts (T65). |
| SL8machine | machine | — | verifier_passes_against_the_checked_in_matrix | T7 | **Mapped now.** This coverage is verified in CI from this checked-in matrix by [`scripts/check-coverage-matrix.sh`](../scripts/check-coverage-matrix.sh); the mapped test ([`crates/cli/tests/coverage_matrix.rs`](../crates/cli/tests/coverage_matrix.rs)) runs that verifier against the real matrix and suite. T7 builds the enforcement; T65 is the full acceptance gate. |
| SL8human | human | — | release-checklist | — | The human-classed criteria — diagram readability (C24), documentation-at-point-of-use (C21), the thirty-minute walkthrough (SL1's human part), and C1's types-readable-from-the-declaration — are on the version-controlled release checklist, reviewed like code. |

## Build-provenance row (Stability, C20)

Build-provenance verification — embedding tool version, git commit, and lockfile
hash in every binary and artifact (arch.md "Stability": Supply chain) — is
**emitted and asserted by T40 (051)**. It is **not** a separate arch.md acceptance
criterion; it is a facet of **C20** (graph artifact) and the Stability
supply-chain commitment, so it does not get its own criterion row (the verifier
requires exactly the partition's id set). It is folded into **C20**'s row above:
the `dagr-cli` build script (`crates/cli/build.rs`) resolves tool version, git
commit SHA, and a lockfile hash at build time and embeds them via `env!`, and the
emitter writes them into the graph-artifact header; the assertion is
`header_carries_the_versioned_provenance_slots` (the header carries the three
provenance fields, fixed per binary and identical across repeated emissions). This
was recorded here as an `unmapped` obligation before it shipped so it would not be
discovered only at the T65 acceptance gate.

## Platform-conditional criteria (for T70)

The rows flagged `platform-conditional` — **C12** (limit detection), **C16**
(signal handling), **C19** (flush/fsync behavior) — remain `machine` (they are
tested), but their pass/fail depends on the platform. T70 attaches the
Linux-tier-1 full-suite and macOS-core-suite jobs and gates these rows per
platform; this matrix names them so that ticket has an unambiguous target.
